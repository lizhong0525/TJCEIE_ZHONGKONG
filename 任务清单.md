# 中控杯决赛任务清单（赛题2：工业多模态感知与无人化智能操作）

> 读法说明：每条都是一个可以打勾的动作。接口名称、字段名都是从官方文档里原样抄的，照抄就能用。
> 三方地址约定：
> - 你的算法服务：`http://127.0.0.1:5000`（被竞赛软件调用）
> - 机械臂 B9 服务：`http://<机械臂主机IP>:8087`（你去调它）
> - 灵巧手 O10 服务：`http://<灵巧手主机IP>:8088`（你去调它）
> - Gemini335 相机：没有 HTTP 文档，自己装奥比中光 SDK 集成
>
> **标记说明**：`【现场】` = 必须有真机/真相机、或到赛场才能干；**没标的** = 在办公室（本机）就能干；`[x]` = 已完成并离线验证过。

---

## 第 0 部分：先搞懂整场怎么跑（10 分钟读完）

- 你要写的东西叫"选手算法服务"，本质就是一个 HTTP 服务器，监听 5000 端口。
- 竞赛操作软件（本地模拟版就是 `测试工具/ZhongkongCup.AlgorithmTester.exe`）是裁判。它**只**会调你 4 个接口：`GET /api/health`、`POST /api/task1/execute`、`POST /api/task2/execute`、`POST /api/task3/execute`。
- 竞赛软件**不看**你返回的识别结果、坐标、路径等任何业务数据，**只看 `success` 字段**（true=这次调用成功，false=失败）。所以识别、规划、抓取全在你自己的服务里完成。
- 请求体永远是空 JSON `{}`，请求头 `Content-Type: application/json; charset=utf-8`。你不用解析请求体。
- 竞赛软件点一下按钮 → 调你的接口 → **同步等待你返回**。你接口里干活花的时间就是比赛用时（同分时按用时排名，所以要么快要么稳）。
- 三个赛题都能让机械臂/灵巧手把道具撞坏，一旦"碰撞造成形变、移位或损毁"，**该任务直接判失败**，比扣分严重得多。所有动作先慢后快。

---

## 第 1 部分：算法服务骨架（你要实现的 4 个接口）

### 1.1 接口规格（照着 `接口示例/contestant_mock_server.py` 写就行）

- [x] `GET /api/health` → 返回 `{"success": true, "message": "ready"}`。竞赛软件用它确认你在线。
- [x] `POST /api/task1/execute` → 赛题1入口。**会被连续调用 3 次**，每次独立处理一次亮灯（详见第 5 部分）。成功返回 `{"success": true, "message": "task1 ok"}`。
- [x] `POST /api/task2/execute` → 赛题2入口。**一次调用要完成全部 4 个长方体的转运**（详见第 6 部分）。
- [x] `POST /api/task3/execute` → 赛题3入口。**一次调用要完成全部 4 个几何体的分拣**（详见第 7 部分）。
- [x] 所有接口加 try/except 兜底：任何异常都返回 `{"success": false, "message": "简短原因"}`，**绝不能让 HTTP 连接挂死或崩溃不返回**。
- [x] 所有接口写日志：调用时间、干了哪几步、成功/失败原因（赛后复盘和争议处理用）。

### 1.2 先用模拟器跑通对接（不碰真机）

- [x] 运行 `python 接口示例/contestant_mock_server.py`（默认 127.0.0.1:5000，全部返回成功）。
- [ ] 打开 `测试工具/ZhongkongCup.AlgorithmTester.exe`，在输入框填 `http://127.0.0.1:5000`。
- [ ] 依次点：健康检查、赛题1、赛题2、赛题3 按钮，确认软件全部显示"调用成功"。
- [ ] 测失败路径：`python contestant_mock_server.py --task2-mode failure`，确认软件能正确显示"失败"。
- [ ] 测耗时容忍度：`python contestant_mock_server.py --task1-delay 8`（模拟 task1 执行 8 秒），确认软件会耐心等。**再测更长的延迟（如 60 秒、120 秒），搞清楚竞赛软件有没有硬超时**——这决定你真机任务能花多长时间。
- [ ] 把 mock 换成你自己的真服务（先返回写死的 success），重复上面步骤，确认你的服务对接没问题。

---

## 第 2 部分：机械臂打通（FTArm B9，`http://<IP>:8087`）

按顺序做，每一步都有"成功判据"，不通过不要往下走。

- [ ] 2.1【现场】`GET /api/status` → 能返回 JSON（`moveit_available: true`），证明服务活着。
- [ ] 2.2【现场】`GET /api/controllers` → `joint_state_available: true`。
- [ ] 2.3【现场】`GET /api/motors` → 每个关节 `fault==0`、`has_feedback==1`、`feedback_age<0.1`。这是"电机健康"的唯一远程判据。
- [ ] 2.4【现场】`POST /api/enable`，请求体 `{}` → 返回 `{"right":{"success":true,...}}`（右臂键名是 `right`）。再调一次 `GET /api/motors` 确认所有关节 `enabled==1`。
- [ ] 2.5【现场】`GET /api/pose` → 拿到当前末端位姿（x/y/z 单位米，roll/pitch/yaw 单位弧度）。刚启动几秒内可能返回 null，等等重试。
- [ ] 2.6【现场】第一次动：先用 `POST /api/end_effector` 加 `"plan_only": true` 预览（只规划不动真机），确认 `success: true`。
- [ ] 2.7【现场】真动：同一个目标去掉 plan_only，加 `"cartesian_linear": true`、`"velocity_scaling": 0.05`（慢）。**成功判据：`message` 是 `Cartesian execution finished...` 且不含 `OMPL`**（含 OMPL = 没走直线，回退了自由路径，要当告警处理）。客户端 timeout 设 ≥90 秒（服务器 60 秒上限）。
- [ ] 2.8【现场】`GET /api/pose` 再确认末端真的到了目标附近。
- [ ] 2.9【现场】`POST /api/joints`（请求体 `{"mode":"right_arm","right_joints":[7个弧度],"velocity_scaling":0.2}`）验证关节空间运动。关节顺序：shoulder_pitch / shoulder_roll / shoulder_yaw / elbow_roll / elbow_yaw / wrist_pitch / wrist_yaw；全零位 = 手臂自然下垂。timeout 设 ≥120 秒。
- [ ] 2.10【现场】验证安全手段：`POST /api/cancel`（只复位软标记，**停不了正在动的轨迹**，真停要用新目标抢占或 disable）；`POST /api/disable`（立即断力，**手臂会因重力下坠**，测试前先把它挪到低位、下方清空）。测完重新 `POST /api/enable`。
- [ ] 2.11【现场】（强烈推荐）示教功能验证：`POST /api/teach_mode {"enable":true}` 进示教（手臂可手拖）→ 拖到关键位置 → `POST /api/teach {"command":"stop"}` → `POST /api/teach {"command":"save","filename":"xxx.csv"}` → `POST /api/teach_mode {"enable":false}` 退出 → `POST /api/playback {"trajectory_id":"xxx.csv"}` 回放验证。赛题里的固定点位（拍照位、槽位上方等）都可以这样采。

### 机械臂关键参数备忘（写代码前背下来）

- 请求体目标位姿必须是**嵌套对象**：`{"mode":"right_arm","right":{"x":..,"y":..,"z":..,"roll":..,"pitch":..,"yaw":..}}`（左臂工作区把 `right_arm`/`right` 换成 `left_arm`/`left`）。
- 推荐通用姿态：`roll=-3.141, pitch=-1.552, yaw=3.141`（真机验证过的）。
- 直线安全工作域（右臂）：Y 在 −0.28 ~ −0.04 米，Z 在 0.44 ~ 0.52 米。越界容易回退 OMPL 或直接失败。
- 默认 `velocity_scaling=0.12`，真机建议 ≤0.3；调试用 0.05。
- 单条 REST 运动指令服务器上限 60 秒——动作要拆小步；长动作改用 WebSocket（`ws://<IP>:8087/ws`，无 60s 限制，还有 progress 推送）。
- 同一时刻只能有一个控制方，服务器不互斥，后发请求会抢占正在执行的运动。

---

## 第 3 部分：灵巧手打通（O10，`http://<IP>:8088`）

- [ ] 3.1【现场】`GET /api/status` → `connected: true`，记下 `hand_type`（`left`/`right`，后面左右手判断要用）和 10 个关节名。
- [ ] 3.2【现场】`GET /api/errors` → 10 个关节错误码全 0。错误码是 bitmask：1=堵转、2=过热、4=过流、8=电机异常、16=通讯异常。
- [ ] 3.3【现场】**第一件事：验证 0 和 1 到底哪个是张手**。文档自相矛盾：§3.1 写"归一化 0=张开，1=闭合"，但 §4.6 示例写 `[1,1,...]=张手`、`[0,0,...]=握拳`。真机各发一次 `POST /api/set_pos`，把结论记下来写进代码注释，后面所有手型都按这个结论来。
- [ ] 3.4【现场】用 `POST /api/set_pos`（请求体 `{"position":[10个0~1的数]}`，阻塞式）标定并固化一组"手型库"：
  - 全张手（抓取前准备）
  - 半开/预抓
  - 夹长方体（赛题2 用，尺寸统一，一个手型通吃）
  - 单指伸出（赛题1 点按钮用：食指直、其余蜷）
  - 拨开关手型（赛题1 拨动开关用）
  - 圆柱/球类包络抓取（赛题3 用）
- [ ] 3.5【现场】（进阶）`POST /api/set_pvc` 带 `torque` 数组（单位 mA，0–1000）做"位置+电流"混合控制，夹持时限制电流，防止夹坏道具或夹飞。
- [ ] 3.6【现场】抓取确认手段：夹持动作后轮询 `GET /api/pvc` 看 `current`（电流）和 `GET /api/errors`，电流升高但位置没到=夹到东西了；位置到底但电流没起=抓空。
- [ ] 3.7 不要用 `POST /api/set_motor` 的极限值（0/4096），会堵转。日常只用 `set_pos`。
- [ ] 3.8【现场】（可选）WebSocket `ws://<IP>:8088/ws` 一连上就有 50ms 周期的状态推送，做实时监控面板用。

### 灵巧手关节顺序（索引 0–9，数组顺序固定）

`0 thumb_roll 拇指旋转 / 1 thumb_abad 拇指外展 / 2 thumb_mcp 拇指弯曲 / 3 index_abad 食指侧摆 / 4 index_pip 食指弯曲 / 5 middle_pip 中指弯曲 / 6 ring_abad 无名指侧摆 / 7 ring_pip 无名指弯曲 / 8 pinky_abad 小指侧摆 / 9 pinky_pip 小指弯曲`

---

## 第 4 部分：相机 Gemini335 + 手眼标定（文档没有，自己搞定）

- [ ] 4.1【现场】安装奥比中光 Orbbec SDK（Python 包 `pyorbbecsdk`），写最小 demo：拿到一帧彩色图 + 一帧深度图 + 相机内参。（装包可以本机先装，但 demo 要插相机）
- [ ] 4.2【现场】相机装在机械臂末端（赛题1 明确"机械臂末端安装 Gemini335 相机"），即 eye-in-hand 配置。
- [ ] 4.3【现场】手眼标定：用标定板（现场允许自备），求出"相机坐标系 → 机械臂末端 → 机械臂 world 基座系"的变换链。机械臂末端位姿用 `GET /api/pose` 取。
- [ ] 4.4【现场】标定验收：拍一个位置已知的物体，解算出它的 world 坐标，和实际位置误差要小于抓取容差（建议 <5mm）。不达标重做标定。（工具已备好：`work/04-相机/tools/verify_known_point.py`）
- [ ] 4.5【现场】封装一个函数：`拍一张照 → 目标在图里的位置 → 输出 world 系三维坐标`。三个赛题全都要用它。（代码已就绪并经合成数据验证，插真相机后需实测确认）

---

## 第 5 部分：赛题1 —— 按钮开关（`POST /api/task1/execute`）

**规则**：电气柜面板上有 3 个灯，每个灯下方有 1 个开关（共 2 个按钮开关 + 1 个拨动开关）。竞赛软件随机点亮一个灯 → 调你一次接口 → 你要把**亮灯下方对应的开关**按掉/拨掉。重复 3 次（3 个灯各亮一次）。
**判分**：每正确完成一个开关操作 30 分 × 3 = 90 分；设备保护 10 分（轻微磕碰一次扣 3 分）。

- [ ] 5.1【现场】标定"拍照位"：机械臂末端（带相机）能看清整个面板的位姿，用示教或手填坐标记下来。
- [x] 5.2 亮灯检测：在拍照位拍照 → 框出 3 个灯的固定区域 → 比较亮度/颜色，输出"哪个灯亮了"。注意每次只亮一个、且不重复。
- [ ] 5.3【现场】标定 3 个开关的 world 坐标和类型（按钮/拨动）。面板是固定的，赛前示教粗标 + 视觉精修最稳。
- [x] 5.4 按钮点按动作序列（写成一个函数）：
  1. `POST /api/end_effector` 到开关正前方 5–10cm（`cartesian_linear: true`，慢速）；
  2. `POST /api/set_pos` 切"单指伸出"手型；
  3. `POST /api/end_effector` 沿按钮方向小幅直线前进（压入几毫米）；
  4. 马上原路后退 5–10cm；
  5. `POST /api/set_pos` 收回手指。
- [x] 5.5 拨动开关动作序列：预抓/伸出姿势 → 接触开关 → 沿拨动方向走一小段 `cartesian_linear` → 反方向撤离。
- [x] 5.6 组装完整一次调用：确认已 enable（2.4）→ 移到拍照位 → 拍照识别哪个灯亮 → 查表得到对应开关坐标和类型 → 执行 5.4 或 5.5 → 撤回安全位 → 返回 `{"success":true,"message":"task1 ok"}`。
- [x] 5.7 **无状态设计**：服务不记"上次灭了哪个灯"，每次调用都重新拍照判断。因为竞赛软件调你 3 次，每次亮哪个灯是现场随机的。
- [x] 5.8 兜底：任何一步失败（规划失败、识别不出、手报错）→ 机械臂先撤回安全位 → 返回 `{"success":false,"message":"原因"}`。演示时每项任务有至多 2 次机会，别在一次里死磕。

---

## 第 6 部分：赛题2 —— 长方体有序转运（`POST /api/task2/execute`）

**规则**：操作台左侧 4 个固定安放槽，每个槽里有 1 个带唯一数字（1–4，每面都有数字）的长方体。**严格按 1→2→3→4 的顺序**抓取，放到右侧指定台面。一次接口调用干完全部 4 个。
**判分**：每个长方体 22 分 = 抓起并离开槽 4 分 + 顺序正确 6 分 + 放到指定台面 8 分 + 放置姿态与槽内姿态一致 4 分；设备保护 12 分（磕碰一次扣 3 分）。

- [ ] 6.1【现场】标定：4 个槽位的 world 坐标（赛前示教记录，槽是固定的）、指定台面放置区的 world 坐标和 4 个落点。
- [x] 6.2 数字识别：拍照 → 检测 4 个长方体 → 识别各自数字（模板匹配 / OCR / 训练一个 1–4 四分类小模型均可，数字印刷清晰、哑光防反光，难度不大）→ 建"数字 → 槽位"映射。（初版通过：01 管线合成图 1–4 全对，06/04 副本已对齐同版；**现场图像集验证未做**）
- [ ] 6.3【现场】标定"夹长方体"手型（`set_pos` 各关节值），以及张手、预抓手型。长方体尺寸统一，一套参数通吃。
- [ ] 6.4【现场】单块转运序列（写成函数，入参=槽位坐标+落点坐标）：（骨架已写好并离线测过；第 5 步"夹稳确认"和第 7 步绕障路径要真机调）
  1. `POST /api/end_effector` 到槽位正上方（留安全高度）；
  2. `POST /api/set_pos` 张手；
  3. `end_effector` 直线下降到抓取高度；
  4. `POST /api/set_pos`（或 `set_pvc` 带 torque）夹持；
  5. 确认夹稳（3.6 的电流/位置判据）；
  6. 直线抬起离开槽（**这一步拿到 4 分**）；
  7. `end_effector` 移到台面落点上方（可走关节运动 `/api/joints` 或分段直线，绕开槽模组）；
  8. 下降到台面 → `set_pos` 松手 → 抬走。
- [ ] 6.5【现场】姿态一致（4 分）：放下时长方体姿态要和它在槽里时一致（槽里竖放就竖放）。规划落点姿态时照搬槽内朝向。
- [x] 6.6 外层循环严格按数字 1→2→3→4。**顺序错了该块的 6 分就没了**，识别没把握就重拍重识别，绝不能猜。
- [x] 6.7 单块失败策略：抓起失败重试 N 次（换抓取高度/手型）；仍失败就返回 `{"success":false,"message":"block 2 grasp failed"}`，把机会留给演示的第 2 次。（重试框架已实现并离线测过，`grasp_retries` 可配；重试时换的高度/手型参数【现场】调）
- [ ] 6.8 注意每条 REST 运动指令 60 秒上限，动作拆小；全程一个控制方串行执行。

---

## 第 7 部分：赛题3 —— 几何体无序分拣（`POST /api/task3/execute`）

**规则**：左侧随机姿态放 4 个几何体（长方体、正方体、多面体、圆柱体等，姿态完全随机：平躺/侧立/翻转/倾斜）。右侧 4 个固定安放槽，槽外侧标注形状名称。识别形状 → 按随机位姿自适应抓取 → 抓起后校正姿态 → 平稳端正放入**对应形状名称**的槽，直到 4 个全部分拣完（不要求顺序）。
**判分**：每个几何体 22 分 = 抓起离开初始区域 6 分 + 放入同名槽 12 分 + 主体完全进入工位边界 4 分（偏移/压线/悬在外面不得分）；安全防护 12 分（磕碰一次扣 3 分）。

- [ ] 7.1【现场】标定：4 个形状槽位的 world 坐标和"形状名称 → 槽位"对应关系（槽是固定的，赛前人工记录即可，**不需要识别槽上的文字**）。
- [ ] 7.2 形状识别：拍照（建议深度图/点云）→ 分割出 4 个物体 → 形状分类。几何特征法（点云平面拟合数量、圆柱拟合）或训练小模型二选一，先在办公室用替代道具验证。
- [ ] 7.3 位姿估计：对每个物体估计 6D 位姿（至少主轴方向/平躺还是侧立），决定从哪个方向抓、抓哪个部位。
- [ ] 7.4【现场】自适应抓取策略库：平躺从上方夹、侧立从侧面夹、圆柱抱抓……每种姿态预设一种手型（`set_pos` 值）+ 接近方向。题目原文有"控制机械臂+灵巧手**调整几何体角度**再抓取"，即允许先拨正再抓。（手型值要真手标定）
- [ ] 7.5【现场】抓取后姿态校正：抓起后物体在手里不一定是正的——用手腕关节（`/api/joints` 调 wrist_pitch/wrist_yaw，或 `end_effector` 改 RPY）在空中把物体转正，再往下放。
- [ ] 7.6【现场】入位（4 分）：落点取槽中心，下降到位再松手，保证主体完全进工位边界、不压线。
- [x] 7.7 循环处理 4 个物体，全部完成再返回。中途掉落=该物体 6+12+4 分没了，但**要接着做剩下的**，别整体中止。
- [ ] 7.8（可选）赛题允许自备"位置标定贴纸"1 张，可用于现场快速校准视觉。

---

## 第 8 部分：健壮性与安全（贯穿所有赛题）

- [x] 8.1 三个 task 接口全部 try/except 兜底 + 详细日志（1.1 已列，再强调一次）。
- [ ] 8.2【现场】每次任务开始先跑"开机自检"：`GET /api/status`（臂）→ `GET /api/motors`（fault 全 0）→ `POST /api/enable` → `GET /api/status`（手，`connected:true`）→ `GET /api/errors`（手，全 0）。任何一步不过就直接返回失败，别带病上阵。
- [ ] 8.3【现场】运动失败分类处理：B9 返回 `All planning strategies failed`（不可达）→ 换路径/降目标；`Motion timeout (60s)` → 提速或拆步；`EXECUTION_FAILED` → 查 `/api/motors`；`message` 含 `OMPL` → 直线没走成，当告警。
- [x] 8.4 灵巧手 `error_codes` 非 0（尤其堵转=1）→ 停手、撤臂、返回失败。（任务层每块前检查 `watcher.first_error`，命中即中止并撤回安全位；离线场景已验证；真机轮询行为【现场】验）
- [x] 8.5 单一控制方：你的服务里同一时刻只能有一个线程在调 B9/O10 的控制接口（两边服务器都不做互斥，后发抢占）。
- [x] 8.6 结束收尾：任务完成或失败后，机械臂回安全位/低位。**不要随手调 `/api/disable`**（掉臂），除非有人托住。
- [ ] 8.7 急停预案贴在桌上：软件急停 = `POST /api/disable`（接受掉臂）；最快 = 直接物理断电/按现场急停。

---

## 第 9 部分：现场流程（非技术，但直接扣分/取消资格）

- [ ] 9.1【现场】2026-08-18 16:00 前到杭州白马湖建国饭店报到（逾期影响参赛资格）。
- [ ] 9.2 带齐：参赛证件 + 身份证、U 盘/移动硬盘（至少 1 个）、笔记本电脑 1 台、标定板（按需）、位置标定贴纸（可选）。
- [ ] 9.3【现场】开赛指令发布后 **30 秒内**点竞赛软件的"开始"按钮，逾期扣 5 分。点了才开始计时。
- [ ] 9.4【现场】设备真出故障：举手示意工作人员，由他们暂停计时；虚假申报 2 次扣 5 分、3 次起共扣 15 分，别乱报。
- [ ] 9.5【现场】演示环节：三个任务**每项至多 2 次操作机会**；演示全程**严禁任何编程调试**，查实直接取消资格。
- [ ] 9.6【现场】自评满意后点"提交" → 输入验证码 **`OK`**（两个大写字母）→ 点"确认交卷"。误触提交可点"恢复竞赛"，但耗时不补。
- [ ] 9.7【现场】交卷后示意工作人员登记总耗时，队长签字；给工控机设锁屏密码并锁屏才能离场，密码一定记住（忘了=放弃后续演示）。
- [ ] 9.8【现场】对成绩有异议：2026-08-20 18:00 前向组委会反馈，逾期不受理。

---

## 附录 A：竞赛软件 → 你的服务（4 个接口）

| 接口 | 方法 | 请求体 | 返回 | 备注 |
|---|---|---|---|---|
| `/api/health` | GET | 无 | `{"success":true,"message":"ready"}` | 在线证明 |
| `/api/task1/execute` | POST | `{}` | `{"success":bool,"message":str}` | 连续调 3 次，每次独立 |
| `/api/task2/execute` | POST | `{}` | 同上 | 一次干完 4 个长方体 |
| `/api/task3/execute` | POST | `{}` | 同上 | 一次干完 4 个几何体 |

软件只看 `success`；请求头 `Content-Type: application/json; charset=utf-8`。

## 附录 B：你 → 机械臂 B9（`http://<IP>:8087`）

| 用途 | 接口 | 方法 |
|---|---|---|
| 系统状态 | `/api/status` | GET |
| 末端位姿 | `/api/pose` | GET |
| 电机诊断 | `/api/motors` | GET |
| 健康检查 | `/api/controllers` | GET |
| 末端运动（自由/直线/圆弧） | `/api/end_effector` | POST（阻塞，60s 上限） |
| 关节运动 | `/api/joints` | POST（阻塞） |
| 使能 / 失能（软急停，掉臂!） | `/api/enable` / `/api/disable` | POST |
| 取消（仅软标记） | `/api/cancel` | POST |
| 控制模式 | `/api/control_mode` | POST |
| 示教开关 / 录制 / 回放 | `/api/teach_mode` / `/api/teach` / `/api/playback` | POST |
| 远程关机 | `/api/shutdown` | POST（需 `confirm:true`） |
| 实时状态+进度 | `ws://<IP>:8087/ws` | WebSocket |

## 附录 C：你 → 灵巧手 O10（`http://<IP>:8088`）

| 用途 | 接口 | 方法 |
|---|---|---|
| 完整状态 | `/api/status` | GET |
| 归一化位置 | `/api/pose` | GET |
| 弧度/速度/电流 | `/api/pvc` | GET |
| 设备配置 | `/api/config` | GET |
| 错误码 | `/api/errors` | GET |
| 归一化位置控制（日常主力） | `/api/set_pos` | POST `{"position":[0~1×10]}` |
| 弧度+电流混合控制 | `/api/set_pvc` | POST `{"position_rad":[…],"torque":[…]}` |
| 电机原始位置（慎用） | `/api/set_motor` | POST `{"motor":[0~4096×10]}` |
| 实时状态推送 | `ws://<IP>:8088/ws` | WebSocket（50ms） |
