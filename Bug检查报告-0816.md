# Bug 检查报告（第二轮，2026-08-16 晚）

测试基线：01 自检 15/15 PASS、02 离线 11/11、03 离线 9/9、04 相机自检 PASS、05 合成 6/6、07 合成 5/5。
**唯一挂掉的是 06-赛题2 合成测试（"数字 2" FAIL）**，深挖后牵出两个真 bug + 一个回归，按严重度排列。

---

## BUG-1（高）：06/04 的 OCR 管线读不出 3 和 4（真实模块对比实验证实）

同一批图（黑底白方块+黑数字，1-4 各一张），两个真实模块的结果：

| 图 | 06 模块（`task2_blocks.py`） | 01 模块（`task2_vision.py`） |
|---|---|---|
| 01 自检四块图 | [1,2,3,4] ✓ | [1,2,3,4] ✓ |
| 单块图 ×4 | 1✓ 2✓ **3✗ 4✗** | **1✓ 2✓ 3✓ 4✓** |

差异在三处：
1. **裁剪**：06 用整块 `boundingRect`（带大量白边）直接喂 OCR；01 用 `minAreaRect` 中心 + `0.6×max边` 的方形紧致裁剪。
2. **预处理**：06 无预处理直接灰度图；01 先 `resize ×2 (INTER_CUBIC)` 再 `阈值 120` 二值化。
3. **psm**：06 用 `--psm 10`（单字符模式，极严格，置信度不够就返回空）；01 用 `--psm 8`。

现场风险：真实赛具照片上的 3/4 完全可能走同一条失败路径 → 漏检 → task2 数量校验直接失败（第 2 次机会打水漂），或数量对但排序错。
**修法**：把 04-相机/vision/task2.py 和 06-赛题2/task2_blocks.py 的 OCR 段对齐 01（方形裁剪 + resize2x + 阈值 + psm 8）。已验证 01 的完整参数在这批图上 1-4 全对。

## BUG-2（中）：06 合成测试的渲染参数是坏的，测试本身在骗人

`test_task2_synthetic.py` 用 `cv2.putText(..., scale=8, thickness=20)` 渲染。实测该渲染的 Hershey 字形 tesseract 5.5 **任何管线都读不对**：

| 渲染 scale8/t20 单字 | 06 模块 | 01 模块 |
|---|---|---|
| 1 | 1 ✓ | **4 ✗** |
| 2 | 空 ✗ | **0 ✗** |
| 3 | 空 ✗ | **5 ✗** |
| 4 | 4 ✓ | 4 ✓ |

即：就算把 06 管线改成 01 管线，这个测试还是过不了（01 会把 2 读成 0、3 读成 5）。之前"5/5 过"的结论要么是旧版 tesseract 的差异、要么是别的机器。**修法**：渲染参数改成 01 自检同款（scale 1.8 / thickness 5，数字画在白方块上，黑底 canvas）——已验证 01 管线该风格 1-4 全对；06 管线对齐 01（BUG-1）后也能全对。

## BUG-3（中，你自己改出来的回归）：task2 重拍逻辑丢弃正确结果

`task2.py:82-83`：

```python
if len(retry) > len(raw):
    raw = retry  # 用识别更多的一次
```

"更多"不是"更对"。场景：第一次**误检 5 个**（噪声轮廓）、重拍**恰好识别出正确的 4 个** → `4 > 5` 为 False → 正确结果被丢弃，报 "got 5 expected 4"。而旧代码 `len(retry) == expected` 会接受它。
**修法**：优先 `len(retry) == expected`，其次取更接近 expected 的：

```python
if len(retry) == expected:
    raw = retry
elif abs(len(retry) - expected) < abs(len(raw) - expected):
    raw = retry
```

## BUG-4（低）：08 mock 与真实服务的空 body 行为不一致

真实服务 `_read_json_or_empty` 对空 body 返回 `{}`（`body_exists=False` 直接过）；mock 对"空 body + application/json"会 `json.loads(b"")` → 400。WPF 联调工具若发空 body，mock 阶段 400、真机阶段 200，排查时容易被误导。
**修法**：mock 的 `_read_json_request` 里 `if content_length == 0: return {}`。

## BUG-5（低）：safe_home 部分标定静默回退

`planner/__init__.py:69-73`：site.yaml 里 `service.safe_home` 只填了两轴、第三轴留占位 → 静默换成内置默认位（0.275, 0, 0.48），警告文案还写"未配置"。半填的坐标和默认位可能差很远，撤回时可能跑偏。
**修法**：三轴全占位才回退；部分填写就抛"第 z 轴未标定"。

---

## 环境（仍没修，顺带提醒）

conda py311 的 ssl 混装问题**依然存在**——`conda list --revisions` 显示今天没有任何改动，`ssl.create_default_context()` 依旧 ASN1 崩溃。本次所有测试是用 shim（把 Windows 证书库加载打成 no-op）跑的，localhost HTTP 不受影响，测试结论有效。**但这台机器上 aiohttp 仍然 import 不了**，比赛前建议真按上一轮给的命令对齐 openssl，或在比赛机上先验证 `python -c "import aiohttp"`。

## 已确认无问题的项（不用动）

- `Vec3.as_tuple()` 全仓库无人调用（占位裸崩路径不存在）。
- task1/task3 流程、errors_watch 中止链、并发 busy、A1/A2/A3 防线：实测全过。
- 02/03/04/05/07 全套测试绿。
