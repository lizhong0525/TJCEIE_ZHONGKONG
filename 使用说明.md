# 中控杯 Gemini335 相机模块使用说明

## 1. 这个文件夹已经实现什么

本模块将相机相关工作拆成六部分：

1. Gemini335 彩色图和深度图同步采集；
2. 深度图对齐到彩色图；
3. 自动读取并保存相机内参和畸变参数；
4. 眼在手上的手眼标定采样、求解与质量报告；
5. 台面深度点云的 RANSAC 平面拟合；
6. 任务一亮灯、任务二数字块、任务三形状的基础检测入口。

所有标定工具默认只读取相机和机械臂当前位置，不会自动移动机械臂。机械臂必须由现场操作员低速移动，并准备好物理急停。

## 2. 文件结构

```text
项目根目录/
├─ config.example.json           可入库的配置模板
├─ config.json                   本机现场参数（首次使用时复制生成，不入库）
├─ requirements.txt              Python依赖
├─ vision/
│  ├─ orbbec_camera.py           Gemini335采集和D2C对齐
│  ├─ geometry.py                像素、相机、末端、基座坐标转换
│  ├─ hand_eye.py                棋盘格识别和眼在手上标定
│  ├─ table_plane.py             台面平面拟合
│  ├─ task1.py                   三灯区域亮度判断
│  ├─ task2.py                   数字块候选检测和OCR入口
│  └─ task3.py                   几何轮廓基础分类
├─ tools/
│  ├─ check_camera.py            实时检查彩色图和深度图
│  ├─ export_intrinsics.py       导出相机内参
│  ├─ collect_hand_eye.py        采集手眼标定样本
│  ├─ solve_hand_eye.py          求解T_end_camera并做质量门禁
│  ├─ fit_table_plane.py         识别台面平面
│  ├─ test_task1_lamp.py         测试任务一亮灯识别
│  ├─ vision_calibrate.py        查看完整标定进度
│  └─ selftest.py                无相机也能运行的离线自检
├─ data/                         真机采样后自动产生
├─ results/                      标定结果自动产生
└─ output/                       检查图片自动产生
```

## 3. 目前什么已经验证，什么还没有

已经在无相机环境验证：

- 配置可读取；
- 像素反投影数学；
- 相机点到机械臂基座点的矩阵链；
- 深度局部中值过滤；
- RANSAC台面平面拟合；
- 全部Python文件语法。

必须拿到真机才能验证：

- Gemini335具体固件与 `pyorbbecsdk2` 是否匹配；
- USB带宽、实际彩色/深度分辨率和对齐效果；
- 棋盘格尺寸与检测效果；
- FTArm实时位姿的坐标约定；
- 手眼标定误差；
- 三灯ROI、OCR阈值、形状阈值；
- 视觉坐标能否让机械臂准确到达目标。

在这些真机验证完成前，结果不得直接用于机械臂运动。

## 4. 第一次安装

先在项目根目录把配置模板复制为本机配置：

```powershell
Copy-Item config.example.json config.json
```

以后只修改 `config.json`，不要把现场 IP、序列号或标定数据写回模板。

模板里的 `task1.lamp_rois_normalized` 故意留空，因为当前比赛文档只说明要识别亮灯，
没有给出所有灯的数量、名称和像素区域。必须根据现场面板或官方完整布局填写；未填写时程序会明确报错，
不会用示例区域误判后控制机械臂。

### 4.1 推荐环境

- Windows 10/11 64位或现场Linux工控机；
- Python 3.10或与奥比中光wheel匹配的版本；
- Gemini335使用USB 3.x接口；
- 机械臂控制电脑和本机位于同一局域网。

### 4.2 创建独立Python环境

在项目根目录打开PowerShell：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

如果现场不能联网，应提前下载所有与现场Python版本匹配的 `.whl` 文件并放到U盘，然后执行：

```powershell
python -m pip install --no-index --find-links .\wheels -r requirements.txt
```

奥比中光官方当前把新版包装为 `pyorbbecsdk2`，代码中的导入名仍为 `pyorbbecsdk`。Gemini335属于Gemini 330系列，应使用SDK v2路线。安装包必须与Python版本和操作系统完全匹配。

### 4.3 Tesseract说明

任务二OCR除Python包外还需要安装Tesseract程序。安装后可将完整路径填入：

```json
"tesseract_command": "C:/Program Files/Tesseract-OCR/tesseract.exe"
```

任务一和手眼标定不依赖Tesseract。

## 5. 第一阶段：确认相机能正常工作

插好Gemini335后双击：

```text
运行相机检查.bat
```

或运行：

```powershell
python tools/check_camera.py
```

窗口中：

- 绿色十字表示图像中心；
- 左上角显示中心深度，单位毫米；
- 按 `S` 保存彩色图和16位深度图；
- 按 `Q` 或 `Esc` 退出。

验收要求：

- 彩色画面正常且没有明显卡顿；
- 深度不是一直 `INVALID`；
- 手掌前后移动时中心深度随之变化；
- `output/color.png` 与 `output/depth_mm.png` 能正常保存。

如果连接多台相机，把目标相机序列号填入 `config.json` 的：

```json
"serial_number": "实际序列号"
```

## 6. 第二阶段：导出相机内参

运行：

```powershell
python tools/export_intrinsics.py
```

结果写到：

```text
results/intrinsics.json
```

其中包括：

- `fx/fy/cx/cy`；
- 图像分辨率；
- `k1/k2/p1/p2/k3`畸变系数；
- OpenCV格式的相机矩阵。

每次更换相机、SDK配置或分辨率后都应重新导出，不能把另一台相机的内参复制过来。

## 7. 第三阶段：眼在手上的手眼标定

### 7.1 准备标定板

默认配置是：

```json
"board_inner_corners": [9, 6],
"square_size_m": 0.025
```

含义是棋盘格有9×6个“内部交点”，每个格子的真实边长是25毫米。这里填的是内部交点数，不是黑白格数量。

必须用卡尺或可靠尺寸确认打印后的实际格长。如果实际是24.7毫米，就必须填 `0.0247`，否则所有三维坐标都会产生比例误差。

标定板应：

- 平整粘贴在硬板上；
- 在整个标定过程中固定不动；
- 无反光、无折痕；
- 所有内角完整进入画面。

### 7.2 填写机械臂地址

修改 `config.json`：

```json
"robot": {
  "base_url": "http://192.168.1.100:8087",
  "arm": "right"
}
```

先用浏览器确认：

```text
http://192.168.1.100:8087/api/pose
```

能够看到非空 `pose` 才能继续。

### 7.3 采集样本

运行：

```powershell
python tools/collect_hand_eye.py
```

程序不会移动机械臂。每次操作：

1. 现场人员低速移动机械臂；
2. 确保相机完整看见棋盘格；
3. 等机械臂完全停止；
4. 在终端按Enter；
5. 程序同时保存当前机械臂位姿、棋盘格相机位姿和预览图。

至少采集12组，建议15到20组。样本必须同时有“位置变化”和“旋转变化”，不能只把机械臂左右平移。建议覆盖：

- 不同距离；
- 左、右、上、下位置；
- 绕不同轴改变角度；
- 棋盘格位于画面中心和边缘。

不要使用模糊、遮挡、重投影误差过大的样本。

### 7.4 求解结果

运行：

```powershell
python tools/solve_hand_eye.py
```

结果写入：

```text
results/hand_eye.json
```

核心字段：

```text
T_end_camera
```

它表示“把彩色相机坐标中的点转换到机械臂末端坐标”的固定4×4矩阵。

程序检查：

- 最大棋盘格重投影误差；
- 标定板在机械臂基座坐标中的位置散布；
- 不同样本计算出的标定板旋转散布。

如果 `quality_gate_passed=false`，该结果不得用于真机，应重新采样。

即使通过，也必须用一个没有参加标定的已知点做独立验证。机械臂应先移动到目标上方安全距离，绝不能第一次就直接接触目标。

## 8. 第四阶段：台面平面识别

先把相机移动到能看清台面的安全位置，并清空 `config.json` 中 `table.roi_normalized` 对应区域内的大型物体。

运行：

```powershell
python tools/fit_table_plane.py
```

结果写入：

```text
results/table_plane_camera.json
```

当前结果在彩色相机坐标系中。后续使用时应结合：

```text
T_base_end × T_end_camera
```

转换到机械臂基座坐标系。

`inlier_ratio`过低表示ROI中障碍物太多、深度噪声太大或ROI选错，程序会拒绝通过。

## 9. 任务一亮灯测试

配置中三个ROI分别对应：

```text
button_1：灯1区域
button_2：灯2区域
toggle：灯3区域
```

ROI格式为：

```text
[左边比例, 上边比例, 右边比例, 下边比例]
```

例如 `[0.05,0.05,0.32,0.45]` 表示图像左上部分。默认值只是占位，必须用现场拍摄图调整。

运行：

```powershell
python tools/test_task1_lamp.py
```

程序输出：

- 被判定为亮灯的目标；
- 三个区域的亮度分数；
- `output/task1_lamp_preview.png`预览图。

必须分别采集“灯1亮、灯2亮、灯3亮”以及不同环境光的数据调节两个阈值：

- `minimum_brightness_score`；
- `minimum_winner_margin`。

比赛任务一是3个灯、2个按钮和1个拨动开关，不是6个按钮。推荐视觉判断灯号并测量面板小偏移，机械动作仍以三套已验证的预设动作作为主体。

## 10. 任务二和任务三说明

`vision/task2.py`已经提供数字块候选和Tesseract OCR入口；`vision/task3.py`提供轮廓、圆度、顶点数和长宽比的基础分类。

它们只是数据采集和初版算法，不是比赛成品。真机后还需要：

- 建立带标注的现场图像集；
- 任务二验证1至4在不同面、角度和曝光下的OCR；
- 任务二估计物体三维姿态并记录初始姿态；
- 任务三确认正式提供的4种几何体；
- 任务三增加三维点云姿态估计、遮挡处理和槽位识别；
- 放置后重新拍照验证结果。

正式赛题是任务二左侧4个初始槽和右侧指定台面；任务三是4个物体和4个对应槽位。

## 11. 坐标转换怎样使用

运行时需要：

1. 图像中的像素 `(u,v)`；
2. 该像素的深度；
3. 相机内参；
4. 机械臂当前末端位姿 `T_base_end`；
5. 手眼标定结果 `T_end_camera`。

计算链是：

```text
像素+深度 → P_camera
P_base = T_base_end × T_end_camera × P_camera
```

代码入口是：

```python
from vision.geometry import pixel_to_base, pose_xyzrpy_to_matrix

t_base_end = pose_xyzrpy_to_matrix(robot_pose)
point_base = pixel_to_base(
    u, v,
    frame.depth_mm,
    frame.intrinsics,
    t_base_end,
    t_end_camera,
)
```

注意：`point_base`只是目标点，不等于安全的机械臂末端位姿。机械动作仍须增加工具尖端偏移、末端姿态、预接近距离、碰撞检查和撤回路径。

## 12. 一键查看标定进度

运行：

```powershell
python tools/vision_calibrate.py
```

它会显示内参、手眼标定和台面平面哪些已经生成，并给出下一步顺序。

主项目的 `algorithm_service/`、`config/site.yaml` 和 `SiteConfig` 已经落地，但 JSON → `site.yaml`
的映射与交叉校验尚未接线（见 TODO.md），所以这里不会猜测 YAML 字段并自动覆盖主配置。
现阶段可靠产物保存在 `results/*.json`。等映射层按主配置真实字段完成并经交叉校验前，
不得把视觉结果直接用于真机运动。

## 13. 离线自检

没有相机时双击：

```text
运行离线自检.bat
```

或运行：

```powershell
python tools/selftest.py
```

显示 `OVERALL PASS` 只表示数学和配置部分通过，不代表真机视觉已经通过。

## 14. 现场验收标准

在接入机械臂运动前，至少满足：

- 彩色与深度稳定采集30分钟；
- 深度已对齐到彩色图；
- 内参来自当前相机和当前流配置；
- 手眼标定质量门禁通过；
- 独立已知点误差满足任务要求；
- 任务一三个灯各测试至少30次；
- 无亮灯、多个疑似亮灯、深度为空时必须拒绝运动；
- 相机或机械臂位姿读取失败时必须返回失败；
- 所有第一次到点均先到目标上方安全距离并低速执行；
- 标定文件、配置文件和原始样本全部备份到U盘。

## 15. 仍需现场确认的信息

1. Gemini335的固件版本和现场操作系统；
2. 现场允许使用的Python版本和SDK安装方式；
3. 相机序列号、实际彩色/深度分辨率及帧率；
4. 棋盘格或标志物是否允许自备；
5. FTArm机械臂IP、端口、左右臂工作区和坐标约定；
6. 相机安装是否牢固、比赛中是否可能被重新安装；
7. 每项任务实际物料的尺寸、材质、颜色及反光情况；
8. 现场调试和重新标定时间；
9. 任务一面板是否在不同操作台之间存在安装偏差；
10. 各任务允许的实际定位误差，而不是统一假定5毫米。

## 16. 安全原则

- 相机识别不确定时，宁可返回失败，也不要猜测坐标后运动；
- 标定板采集工具不自动移动机械臂；
- 所有运动必须经过独立规划器、安全区和碰撞检查；
- 软件停止等待不等于机械臂停止运动；
- `/api/disable`可能导致机械臂掉落，不能当作普通停止按钮；
- 物理急停优先于任何软件命令。
