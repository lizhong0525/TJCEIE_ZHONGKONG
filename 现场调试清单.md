# 现场调试清单（标定日照着做）

11 步顺序固定，**前一步不过别跳下一步**。优先级：赛题 2 > 赛题 1 > 赛题 3。
每步附预期输出和常见失败原因。工具位置：`01-算法服务/tools/`（site_check / debug_console）、
`02-机械臂/arm_bringup.py`、`03-灵巧手/hand_bringup.py`、`04-相机/tools/`。

| # | 步骤 | 命令 / 操作 | 预期 | 常见失败 |
|---|------|------------|------|---------|
| 1 | 算法服务健康 | `curl http://127.0.0.1:5000/api/health` | `success:true` | 服务没起；`import aiohttp` 崩（ssl 混装机要先验） |
| 2 | 机械臂连通 | `curl http://<臂IP>:8087/api/status` | 返回 JSON | IP/网线；8087 被占 |
| 3 | 灵巧手连通 | `curl http://<手IP>:8088/api/status` | 返回 JSON | 同上；**先确认手是 O10 还是 DexHand（端口/字段不同）** |
| 4 | 臂自检 | `python 02-机械臂/arm_bringup.py --host <臂IP>` | status→motors→enable 全过 | TF 未就绪等几秒；enable 失败查急停 |
| 5 | 手自检 | `python 03-灵巧手/hand_bringup.py --host <手IP>` | errors 全 0 + **0/1 方向人眼确认** | 方向文档自相矛盾，必须真机看一次 |
| 6 | 相机取图 | `python 04-相机/tools/check_camera.py --save` | 窗口出彩图，output/ 存图 | pyorbbecsdk2 没装；USB 供电不足 |
| 7 | 标定链 | 内参 → 采 ≥12 组手眼（角度要散开）→ `solve_hand_eye` 过门禁 → `verify_known_point` 验收 | 门禁 PASS + 已知点误差内 | 样本角度太集中求解不准；**rotation spread >2° 会被门禁拦** |
| 8 | 填 site.yaml | 用 `tools/debug_console.py` 读位姿直接打印可粘贴格式 | 所有占位填完 | 手誊坐标抄错位；手眼填 `t_end_camera` 原值即可 |
| 9 | 未标定清零 | `curl http://127.0.0.1:5000/api/config/summary` | `uncalibrated: []` 且 `dryRun:false` | 漏填项全列在这个端点里 |
| 10 | 赛题 2 单块→全链 | debug_console 模拟竞赛软件调 `POST /api/task2/execute` | success:true，4 块按 1→2→3→4 入槽 | OCR 读错（换角度/曝光重拍）；数量不符看 JSONL |
| 11 | 赛题 1 → 赛题 3 | 同上依次调 task1 / task3 | success:true | 灯 ROI 阈值；形状名与 kinds 对不上会全跳过报错 |

## 上场前最后核对（30 秒）

1. `GET /api/config/summary` → `uncalibrated` 为空、`dryRun` 是 **false**（team02 的坑：dry_run 忘关，接口全 success 但机器人不动）。
2. 服务启动日志**没有** `⚠️ DRY-RUN` 大红字。
3. `logs/今天日期.jsonl` 在持续写入。
4. 物理急停按钮位置所有人都知道。

## 排障先看哪

- 任务失败 → `logs/YYYYMMDD.jsonl` 最后一行的 `result_message`（人话原因）+ `elapsed_ms`。
- 服务行为怪 → `GET /api/config/summary` 看 dryRun / 未标定清单。
- 相机/手眼坐标错 → 04 的 `verify_known_point.py` 重验，别直接改代码。
