# 遗留问题

所有未闭环的风险、待办和"还剩啥"，按杀伤力排（🔴 致命 / 🟠 重创 / 🟡 轻伤）。
历史审查报告（Bug检查报告-0816、代码审查-屎山排查、魔鬼清单-失败点、team01/team02 对比参考、
修改点清单、文档核对报告、决赛提交对比、旧进度总览）的现役内容已全部并入本文件，
原件自 2026-08-17 起删除不再维护。

> **2026-08-18 审查修复批次**：6 子代理审查报告（屎山/Bug）已逐项核对并修复落盘——
> P0-1/P0-2/P0-3/P0-4、P1-5~P1-11、P2-12/14/15（除 safe_box 大小校验）全部完成，
> 另修复报告新增的 🔴-1~🔴-4、🟠-1~🟠-5、🟠-9。01 自检 33 项全绿、7.2 pytest 11 项、
> 02~07 模块测试全过。**本批改动尚未推回 GitHub**（推拉前先和对方确认）。
> 报告两条不实结论已证伪：六棱柱 0°/30°"必误判"（实测 fill≈0.737 压线通过，
> 窗口已放宽到 0.76 留裕量）；grasp_sorting_project 目录不存在（实际存在）。

## 一、🔴 致命级（直接废一场）

1. **dry_run 忘关 / 旧版 site.yaml 覆盖回去** → 接口全 success、机器人不动，三场白跑。
   防线有五道（启动红字 / site_check / config/summary / **health 响应 dryRun 标记（新增）** /
   上场前核对），但仍全收敛到"有人核对"。
2. **灵巧手型号没确认**（O10 `:8088` 字段 `position` vs DexHand `:5001` 字段 `positions`）→
   手全崩三赛题全灭。开赛第一件事（操作指南 §5 第 0 步）。
   **P0-1 已修**：`status()` 缺 connected/hand_type 键即抛"疑似 DexHand"（带响应 keys）；
   `pose()` 兼容 position/positions；`errors()` 缺 error_codes 返回哨兵 [-1]*10
   （watcher 会当中止，8.4 不再静默失效）；site_check 加 hand_type/长度断言，不再假绿。
3. **环境装错/混装**（比赛机无外网，装错不可修）：官方 Python 3.11.9 + 6 包精确版本 wheels
   （安装列表见 `比赛机环境清单.md`；conda 方案已废弃，比赛机不装 conda——PATH 混入 conda 的 python
   会 ssl/ASN1 崩）。**现场安装冒烟已扩**（ssl + contrib 断言 + numpy 1.x 断言 +
   Python 3.11/3.12 断言 + pyorbbecsdk import + tesseract 主版本 ≥5）。
4. **safe_box / 手眼矩阵标错**（比不标更危险）→ 错误坐标放行 → 物理碰撞直接判负。
   **手眼整矩阵校验已加**（末行 [0,0,0,1] / 旋转块 det≈1 / 平移 <1m / 全有限值，
   不再是只查 he[0][0]）；verify_known_point + calibrate 录入校验兜底；
   safe_box 本身仍无人交叉验证（大小关系校验未做）。
5. **pyorbbecsdk2 与相机固件不匹配 / USB 供电不足** → camera not ready，三赛题全灭，
   现场不可修。现场安装冒烟已能查出"SDK 装没装对"。
6. **左右臂装反 / 坐标符号翻错** → 镜像坐标撞机。**bringup 已加 5b 坐标方向断言**
   （right 臂 y≥0.05 即 FAIL 报"疑似装反"）——旧文档"plan_only 能发现装反"是谎言
   （plan_only 目标是当前位姿，与物理臂无关），现已修正。
7. **现场网络不稳**（WiFi 干扰）→ HTTP 全超时。**查询类 GET 已加一次重试**
   （运动 POST 绝不重试）；网络差时仍等同 🔴。

## 二、🟠 重创级（静默丢大分）

1. **task1 亮灯并列拒绝已修（P0-2）**：`detect_lit_lamp` 全 ROI 计分排名，
   前两名差 < `minimum_winner_margin`（task1.yaml，默认 1.2）→ 报"亮灯结果不唯一"拒动。
   **现场必须用图像集实测调阈值**（得分是相对阈值倍数，1.2 是拍的）；亮度兜底
   `mean_v>=140` 被环境光骗的风险仍在。
2. **OCR 对字体敏感**：数字带衬线/艺术字可能静默读错（1↔7、2↔3），顺序分 24 分全丢。
   已缓解：1–4 数字过滤（0/5/9 丢弃）、无 eng 明确失败（不再"也试跑"）、
   超时 5s 捕获返回 None（不再裸抛 TimeoutExpired 炸全场）、
   `digit_blocks.tesseract_command` 可指定 exe 路径。字体误读本身无置信度防线。
3. **task3 形状分类极限**：2D 分类应对随机姿态有极限（平躺圆柱俯视=矩形）。
   **已修**：兜底改 `unknown` + task3 unknown 门禁跳过（不再静默塞进长方体槽）；
   六棱柱 fill 窗口 0.74→0.76（0°/30° 摆放实测压线，已留裕量）；
   噪声圆 circularity 压 0.90 阈值线仍在（conf 自然偏低但不拦截，**现场图像集必测
   圆柱漏判**——现在漏判会跳过分拣而不是放错槽）。7.4 自适应手型库未做。
4. **60s REST 硬上限**：长动作已拆小步但无实测；竞赛软件超时容忍度实测未做
   （操作指南 §2 第 4 步）；busy 拒绝会浪费一次机会（共 2 次）。
5. **拍照与读位姿的时差**：capture 先拍照、后读 `/api/pose` 取 t_base_end——臂有运动
   残留/振动时姿态差 1–2mm → 坐标偏，无防线。（未修，真机 verify_known_point 量化）

## 三、🟡 轻伤级

- `expected_count` 填错（3 或 5）→ 明确失败但浪费一次机会（已有 ≠slots 交叉告警）。
- safe_box 大小关系校验（min>max 等）未做；`OMPL` 告警关键词已扩 OMPL/RRT 且
  **line_to 直接抛 ArmError 按失败撤回**（不再是只写日志）。
- task3 物体姿态 obj_rpy 只作记录，7.5 空中姿态校正未接运动链。
- P2-13：calibrate 补 `safe_home`/`default_rpy`/`log_dir`/`dry_run` 录入交互未做。
- 04 相机 `orbbec_camera.py` 的 `aligned.as_frame_set()` 是全仓唯一用法——
  **现场第一件事跑 `运行相机检查.bat`** 验证该绑定存在；solve_hand_eye 门禁失败
  仍写 hand_eye.json（带 false 标志，人工转填时别误用）。
- 05/06/07 模块副本与 01 的漂移已收敛（task1 并列拒绝、OCR 过滤进 01；
  04 task2 副本同步超时捕获/无 eng 拒跑）；04/05/06/07 四份副本逐字节一致。

## 四、待核实事实冲突（开赛拿官方文档确认）

1. **灵巧手型号**：O10 vs DexHand 二选一——代码已加结构校验会主动炸，但确认后仍要全仓统一。
2. **平台是否轮询 `GET /`**：已加根路由返回 ready，无害，留观。

## 五、赛前待办（2026-08-18 批次后剩余）

**已完成的 P0/P1 不再列出（见顶部批次说明）。剩余：**

1. **P2-13**：calibrate 补 service 段（safe_home/default_rpy/log_dir/dry_run）录入交互。
2. safe_box 大小关系校验（x_min<x_max 等，配置加载时 warning）。
3. **现场图像集验证（最重要）**：task1 margin 阈值实测、圆柱/六棱柱在各姿态下的
   分类稳定性、task2 OCR 现场字体、相机 D2C 链（01 生产版新对齐 04 的
   frame_sync + AlignFilter + depth_scale + 尺寸守卫，**真机首验**）。
4. 本批改动跑完全量回归后**推回 GitHub**（先和对方确认）。

## 六、各模块"还剩啥"（2026-08-18 现状）

- **01 算法服务**：`04 results/*.json → site.yaml` 填值接线（手眼链有整矩阵校验）；
  手型标定（site.yaml hand.poses 四个任务手型现为占位，未标定任务层拒动指名）；
  相机采集链 D2C 真机首验；task1 margin 阈值现场调；真机联调。
- **02 机械臂**：全真机——bringup 实跑（新 5b 方向断言）、60s 上限行为、TF 等待重试。
- **03 灵巧手**：全真机——型号确认（结构校验会主动炸）、0/1 方向验证、
  4 个任务手型标定、set_pvc 限流调参、电流抓取确认实测。
- **04 相机**：全真机——装 pyorbbecsdk2、导内参、手眼 ≥12 组过门禁、
  **verify_known_point 验收（不许跳）**、task1 ROI 标定、task2/3 现场图像集迭代。
  还需现场确认：相机固件/SDK 路线、序列号与分辨率帧率、棋盘格可否自备、
  臂 IP 与坐标约定、相机安装是否牢固、物料尺寸材质反光、各任务允许的实际定位误差。
- **05/06/07（模块工作台）**：现场标定 + 算法迭代；06 记每块槽内初始姿态（4 分姿态分）；
  07 文件夹仍是 2D 模块副本（01 已整合 7.2/7.3 + unknown 门禁）。
- **08 联调**：WPF 超时实测；真服务替换 mock 后的全流程验收。
- **09 部署**：安装列表方案已定稿（python-3.11.9 官方安装器 + 6 包精确版本 wheels，`打包conda环境.bat` 已废弃）。
  **待办：有网机跑 `下载wheels.bat` 生成 wheels/ 并核对 6 个精确版本文件齐全**；确认比赛机 Python 3.11.9。

## 七、三条红线（全程有效）

1. 视觉没把握宁可返回失败，也别猜坐标去动臂。
2. `/api/disable` 会掉臂，不是普通停止键；物理急停优先于一切软件命令。
3. 演示环节严禁编程调试——所有防线都收敛到"赛前有人按操作指南核对"这一个动作上。
