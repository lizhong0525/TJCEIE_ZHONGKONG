# 遗留问题

所有未闭环的风险、待办和"还剩啥"，按杀伤力排（🔴 致命 / 🟠 重创 / 🟡 轻伤）。
历史审查报告（Bug检查报告-0816、代码审查-屎山排查、魔鬼清单-失败点、team01/team02 对比参考、
修改点清单、文档核对报告、决赛提交对比、旧进度总览）的现役内容已全部并入本文件，
原件自 2026-08-17 起删除不再维护。

## 一、🔴 致命级（直接废一场）

1. **dry_run 忘关 / 旧版 site.yaml 覆盖回去** → 接口全 success、机器人不动，三场白跑。
   防线有四道（启动红字 / site_check / config/summary / 上场前核对），但全收敛到"有人核对"。
2. **灵巧手型号没确认**（O10 `:8088` 字段 `position` vs DexHand `:5001` 字段 `positions`）→
   手全崩三赛题全灭。开赛第一件事（操作指南 §5 第 0 步）。
3. **conda 混装**（python 与 openssl 不同渠道）→ `import aiohttp` 崩，无外网修不了。
   用 09 的 conda-pack 完整包就碰不到；现场安装冒烟专查这条。
4. **safe_box / 手眼矩阵标错**（比不标更危险）→ 错误坐标放行 → 物理碰撞直接判负。
   verify_known_point + calibrate 录入校验兜底，但 safe_box 本身无人交叉验证（待办 15）。
5. **pyorbbecsdk2 与相机固件不匹配 / USB 供电不足** → camera not ready，三赛题全灭，现场不可修。
6. **左右臂装反 / 坐标符号翻错** → 镜像坐标撞机。bringup 的 plan_only 能提前发现，无自动化验证。
7. **现场网络不稳**（WiFi 干扰）→ HTTP 全超时（臂 90s / 手 10s），查询类无重试。
   现场网络差时等同 🔴（待办 11 只缓解）。

## 二、🟠 重创级（静默丢大分）

1. **task1 亮灯无并列拒绝**：01 版 `detect_lit_lamp` 直接取最高分、无 margin 检查；
   反光两灯同亮 → 静默选错灯丢 30 分（05 模块版有并列拒绝，但生产以 01 为准——坑就在这）。
   亮度兜底 `mean_v>=140` 也会被环境光骗。→ 待办 P0-2。
2. **OCR 对字体敏感**：数字带衬线/艺术字可能静默读错（1↔7、2↔3），顺序分 24 分全丢且
   数量校验照过；无置信度检查。tesseract 旧版（4.x）或只有 chi_sim 同罪；
   比赛机若恰好有个无 eng 数据的 `D:\OCR\tesseract.exe` 会被候选链优先命中。
3. **task3 形状分类极限**：2D 分类应对随机姿态有极限（平躺圆柱俯视=矩形）；
   7.3 点云位姿（质心+PCA）已接入 01 但**真机未验证**；7.2 噪声圆 circularity 压 0.90 阈值线
   （`test_noisy_circle` 无随机种子偶败）——**现场图像集必测圆柱漏判**。
   7.4 自适应手型库（平躺上夹/侧立侧夹/圆柱抱抓）未做。
4. **60s REST 硬上限**：长动作已拆小步但无实测；竞赛软件超时容忍度实测未做
   （操作指南 §2 第 4 步）；busy 拒绝会浪费一次机会（共 2 次）。
5. **tesseract 子进程 5s 超时**：比赛机 CPU 慢 → 每块 OCR 超时返回 None → 数量校验失败；
   4 块就是 20s+ 的隐性地雷，无重试。
6. **拍照与读位姿的时差**：capture 先拍照、后读 `/api/pose` 取 t_base_end——臂有运动残留/振动
   时姿态差 1–2mm → 坐标偏，无防线。

## 三、🟡 轻伤级

- JSONL 日志不轮转不清理（>20MB 建议轮转；比赛当天量小，但拷 U 盘跟着走）。
- `OMPL` 关键词告警：厂家改文案（如 RRT）→ 静默不告警（待办 15 扩展关键词）。
- `hand.errors()` 假设 `error_codes` 字段：固件改名 → watcher 线程挂 → 8.4 失效且无感。
- `_to_float` 对 nan/inf、`"0.0l"` 之类转占位 → 启动不报错、任务时才拒动，
  现场表现为"标定了还是拒动"（待办 9）。
- `expected_count` 填错（3 或 5）→ 明确失败但浪费一次机会。
- `placement_order_target` 拼错（"desending"）→ 静默反序丢 24 分（待办 6）。
- 中文路径 `cv2.imwrite` 可能静默失败（返回 False），debug_console 存图未查返回值（待办 14）。

## 四、待核实事实冲突（开赛拿官方文档确认）

1. **灵巧手型号**：O10 vs DexHand 二选一，确认后全仓统一。
2. **平台是否轮询 `GET /`**：已加根路由返回 ready，无害，留观。

## 五、赛前待办（P0 必做 / P1 强烈建议 / P2 顺手；预计约 2 小时，做完跑全量回归）

**P0（防致命）**

1. **灵巧手型号自适应校验**：`hand.py` / 03 `hand_client.py` 的 `status()` 加结构校验
   （缺 `connected`/`hand_type` 抛 `HandError("疑似 DexHand 或固件不同：<keys>")`）；
   `pose()` 兼容 `position`/`positions` 两字段；hand_bringup 第 1 步打印响应 keys 主动比对。
   **全场最大单点，优先于一切。**
2. **task1 亮灯加"并列拒绝"**：01 的 `detect_lit_lamp` 取前两名、`margin < 阈值`
   （读 task1.yaml `minimum_winner_margin`，默认 1.2）→ 报"亮灯结果不唯一"拒动。
3. **环境版本断言**：现场安装.bat 冒烟扩成查 `cv2.findChessboardCornersSB` 存在（需 contrib）、
   numpy 1.x、Python 3.11/3.12；tesseract `--version` 主版本 ≥5（4.x 的 psm 8 行为不同，
   可能读错数字而不是读空）。
4. **calibrate 录入防手滑**：`_ask_vec3` 范围提示（|y|>0.5m / z>0.7m 提醒）、`travel` 提示
   "单位米，0.005=5mm"、落盘前打印全部数值摘要 + 二次确认。

**P1（防静默丢分）**

5. 01 的 OCR 数字过滤 `digit not in (1,2,3,4) → None`（与 06/04 对齐，防 0/5/9 误读静默排错序）。
6. `placement_order_target` 取值校验（拼错静默反序；建议 warning + 归正 ascending）。
7. `expected_count` 已标定且 ≠ slots 数量 → warning 交叉告警。
8. tesseract 候选链去掉"都不带 eng 也试跑"的兜底，改为返回 None（无 eng = 明确失败，比乱读好）。
9. `_to_float` 拒绝 nan/inf（记 warning + 按占位符处理，未标定清单能看见）。
10. JSONL 日志大小上限（>20MB 轮转，防磁盘写满）。
11. 查询类 GET 请求挂 urllib3 Retry(total=2)（**运动 POST 绝不重试**）；`hand.errors()` watcher 尤其受益。

**P2（顺手）**

12. `config.py` HandEye docstring 改 "T_end_camera（相机→末端）；完整链 = t_base_end @ t_end_camera"
    （现注释还写着"基座→相机"，会误导接线）。
13. calibrate.py 补 `safe_home`/`default_rpy`/`log_dir`/`dry_run` 录入交互。
14. debug_console.py：存图检查 `cv2.imwrite` 返回值；`except (ArmError, Exception)` 冗余写法清理。
15. safe_box 大小关系校验、OMPL 关键词扩展（RRT/回退）、task2 同数字排序加第二键 `b.pick.x`、
    health 响应加 dryRun 标记。

## 六、各模块"还剩啥"（2026-08-17 现状）

- **01 算法服务**：`04 results/*.json → site.yaml` 填值接线（链代码已统一为
  `t_base_end @ t_end_camera`，接线前视觉结果不得用于真机）；task1 两套亮灯检测现场二选一
  （生产以 01 为准，05 版有并列拒绝）；真机联调。
- **02 机械臂**：全真机——bringup 实跑、message 文本与文档核对、60s 上限行为、TF 未就绪等待。
- **03 灵巧手**：全真机——型号确认、0/1 方向验证、5 个手型标定、set_pvc 限流调参、电流抓取确认实测。
- **04 相机**：全真机——装 pyorbbecsdk2、导内参、手眼 ≥12 组过门禁、**verify_known_point 验收（不许跳）**、
  task1 ROI 标定、task2/3 现场图像集迭代。还需现场确认：相机固件/SDK 路线、序列号与分辨率帧率、
  棋盘格可否自备、臂 IP 与坐标约定、相机安装是否牢固、物料尺寸材质反光、各任务允许的实际定位误差。
- **05/06/07（模块工作台）**：现场标定 + 算法迭代；06 记每块槽内初始姿态（4 分姿态分）；
  07 的 3D 位姿已接入 01（真机实测是最大工作量），07 文件夹仍是 2D 模块副本。
- **08 联调**：WPF 超时实测；真服务替换 mock 后的全流程验收。
- **09 部署**：在家跑 `打包conda环境.bat` 生成 `zkc_env.tar.gz`（**当前未生成**）+ `下载wheels.bat` 备选；
  确认比赛机 Python 版本（代码要求 3.11/3.12）。

## 七、三条红线（全程有效）

1. 视觉没把握宁可返回失败，也别猜坐标去动臂。
2. `/api/disable` 会掉臂，不是普通停止键；物理急停优先于一切软件命令。
3. 演示环节严禁编程调试——所有防线都收敛到"赛前有人按操作指南核对"这一个动作上。
