# 魔鬼清单：项目可能被操控导致失败的点（红队视角）

> 视角：假设有"魔鬼"（环境坑、手滑、恶意配置、疏忽、他队干扰）想让你拿 0 分。
> 按杀伤力排序：🔴=致命（直接废一场/白跑）、🟠=重创（丢大分或物理风险）、🟡=轻伤。
> 每条标注：有没有防线（✅有 / ⚠️半吊子 / ❌没有）。

---

## 一、🔴 一个文件毁全场：site.yaml 是最大单点

### S1. dry_run 忘关 / 被改回 true —— 接口全 success，机器人不动，白跑一场
- 防线：✅ 启动大红字警告 + `site_check.py` 检查 + `config/summary` 回显 + 现场调试清单"上场前 30 秒核对"。
- 魔鬼操作：**把旧版 site.yaml（dry_run: true 的版本）覆盖回去**，或比赛前 git 回滚。四道防线全依赖"有人跑 site_check"——没人跑就全废。
- 杀伤力：🔴 三场全白跑，且竞赛软件显示成功，你毫无察觉。

### S2. safe_box 标错 —— 比不标更危险
- 不标（占位）→ `ensure_safe` 拒动，安全方向。**标错** → 错误坐标被放行 → 机械臂往面板/槽模组上怼 → 碰撞=该任务直接判失败。
- 防线：✅ 只有 verify_known_point 能抓（它验证的是坐标链，但 safe_box 本身没人交叉验证）。
- 魔鬼操作：把 y_min/y_max 写宽 5cm，让"往 Y 正方向飞"的目标通过校验。
- 杀伤力：🔴 物理碰撞，设备保护分扣光 + 任务判失败。

### S3. 坐标/单位手滑（漏负号、m/mm 混淆、travel 十倍）
- `y: 0.20` vs `y: -0.20` → 臂去镜像位置；`travel: 0.05`（应为 0.005）→ 手指怼进按钮 5cm。
- 防线：❌ **calibrate.py 交互录入没有单位/符号合理性校验**；`safe_box` 若同步标错就全放行。
- 魔鬼操作：手滑是最现实的魔鬼。
- 杀伤力：🟠 物理风险或任务失败。

### S4. 非法数值绕过占位符检查
- `_to_float` 对 `nan`/`inf` 直接放行（float() 成功）；`is_placeholder` 对 NaN 返回 False。
- 后果：NaN 进手眼矩阵 → 坐标变 NaN → `safe_box.contains` 对 NaN 恒 False → **拒动**（安全方向，服务瘫痪但不危险）；NaN 进 safe_box → 所有目标"越界" → 全拒动。
- 防线：⚠️ 会拒动不会乱动，但现场表现为"莫名其妙全失败"，排障困难。
- 杀伤力：🟡 瘫痪型失败。

### S5. `placement_order_target` 拼错（"desending"）→ 顺序反着放，6 分×4 全丢
- config 对该字段**零校验**（不查取值枚举）。
- 杀伤力：🟠 静默丢 24 分，success=true 你都不知道。

### S6. `expected_count` 填错（3 或 5）→ 数量校验过不了或漏放
- 填 3：识别 4 个 → 重拍 → 还是 4 → 失败，浪费机会；填 5：永远识别不满 → 失败。
- 防线：✅ 至少会明确失败（A3 防线），不会静默。
- 杀伤力：🟡。

---

## 二、🔴 环境炸弹：比赛机无外网，错一个依赖全灭

### E1. conda ssl 混装（本机已踩过）
- `import aiohttp` 直接崩 → 服务起不来。**比赛机若用 conda 环境就是灾难**。
- 防线：✅ 09 README 已知坑 + `现场安装.bat` 第 2 步冒烟检查。❌ 但你自己的开发机到现在还没修。
- 杀伤力：🔴。

### E2. tesseract 版本/语言数据
- 装了但只有 chi_sim → `_tesseract_cmd` **退而求其次"至少试跑"** → OCR 输出乱码/空 → task2 全灭。
- 更阴的：**装了个旧版 tesseract（4.x）** → psm 8 行为不同 → 可能**读错数字而不是读空** → 静默排错序（顺序分 24 分全丢），数量校验还通过。
- 防线：⚠️ eng 检查有，但"退而求其次"的兜底是魔鬼通道；版本差异无检查。
- 杀伤力：🟠。

### E3. opencv-python（非 contrib）/ numpy 2.x / Python 版本错位
- `findChessboardCornersSB` 只有 contrib 有 → 标定工具 AttributeError；numpy 2.x 破坏性变更未验证；现场 Python 3.10 → 依赖装不上。
- 防线：✅ requirements 写对了；❌ 无运行前版本断言（site_check 不查版本）。
- 杀伤力：🟠 标定日半天白费。

### E4. pyorbbecsdk2 与固件/相机不匹配
- 取帧失败 → "camera not ready" → 三赛题全灭。USB 供电不足是经典暗坑。
- 防线：✅ check_camera 第 6 步。
- 杀伤力：🔴（现场不可修，只能祈祷）。

### E5. 中文路径 + cv2.imwrite 静默失败
- 项目在 `G:\中控杯\0816\work\`（中文路径）。`debug_console.py` 拍照存图到中文路径下——**cv2.imwrite 对非 ASCII 路径在 Windows 上可能静默失败**（返回 False），代码没检查返回值 → 提示"已存"实际没存。
- 杀伤力：🟡 现场调参时"图呢？"。

---

## 三、🔴 未决事实：开赛第一件事，错了全崩

### F1. 灵巧手型号：O10（:8088，字段 position）vs DexHand（:5001，字段 positions）
- 进度总览/team01 对比都标了"待核实事实冲突"。**hand_client 全部字段按 O10 写的**——现场若是 DexHand：status/errors/set_pos 全对不上 → 手全崩 → 三赛题全灭。
- 防线：❌ 只有现场调试清单第 3 步"先确认型号"。
- 杀伤力：🔴🔴 全场最大单点。

### F2. 机械臂左右手/安装方向
- arm_side right/left + 坐标符号——装反了 → 镜像坐标 → 撞。
- 防线：⚠️ bringup 第 6 步 plan_only 能提前发现；❌ 没有自动化验证。
- 杀伤力：🔴 物理。

### F3. IP/DHCP 变动
- 臂/手 IP 写死 site.yaml。现场 DHCP 换 IP → 全连不上。
- 防线：✅ site_check 汇总能看到。
- 杀伤力：🟠。

---

## 四、🟠 视觉算法盲区（真机才暴露）

### V1. task1 两套亮灯检测行为不一致（现场二选一的坑）
- 05 版有"并列拒绝"（margin 检查），**01 版没有**——01 直接取最高分。
- 现场反光导致两灯都亮 → 01 版**静默选错灯** → 按错开关 → 30 分丢；05 版会拒绝。二选一选 01 就踩。
- 防线：❌ 无 margin 检查的 01 版是生产路径（default_runner 用的是 task1.run → task1_vision）。
- 杀伤力：🟠 每次可能丢 30 分。

### V2. task1 亮度兜底被环境光骗
- `mean_v >= 140` 灰度均值兜底——白天窗光/面板反光 → 全亮 → 选错。
- 防线：⚠️ ROI 标小 + 现场调阈值（任务清单 5.3 未做）。
- 杀伤力：🟠。

### V3. OCR 对印刷字体敏感（数字 1-4 的字体未知）
- 数字若带衬线/艺术字（7 带横、1 带底）→ 静默读错 → 排序分 24 分全丢，数量校验还通过。
- 防线：❌ 无置信度检查；`digit not in (1,2,3,4)` 过滤只挡 0/5/9，挡不住 1↔7、2↔3 这类形近误读。
- 杀伤力：🟠 静默。

### V4. task3 2D 分类的已知极限
- 平躺圆柱俯视=矩形 → 放进错槽（12 分×4 可能全丢）；正方体 45° 俯视=菱形 → vertices=4 但 ratio>1.2 → 判错。
- 防线：✅ 文档诚实记录了；❌ 3D 位姿估计未做（最大技术缺口，第 7.2/7.3 未勾）。
- 杀伤力：🟠🟠 全场最大技术风险。

### V5. 拍照与读位姿的时差
- capture() 先拍照、再 arm.pose() 读 t_base_end——臂若有运动残留/振动 → 姿态差 1-2mm → 坐标偏。手眼标定验证（verify_known_point）能暴露，但那是标定时刻，不是任务时刻。
- 防线：⚠️ 无。
- 杀伤力：🟡。

---

## 五、🟠 时序炸弹（同分按用时排名，超时=失败）

### T1. 60s REST 硬上限 + 无 WebSocket 实现
- 任务清单明确写了长动作要拆小步/用 WS；代码只有 REST。task3 绕障路径一旦超 60s → 服务端断 → ArmError → 任务失败。
- 防线：⚠️ 动作库拆了小步，但无实测；WPF 超时容忍度测试（08 第 5 步）未做。
- 杀伤力：🟠。

### T2. 网络延迟/抖动 → 全部 HTTP 超时
- 臂 timeout=90s、手 timeout=10s、相机 1s——**网络是单点**：丢包一次 → 手请求卡 10s → 任务时间暴涨或失败。
- 防线：❌ 无重试（除 task2 抓取）。比赛现场 WiFi 干扰/人多 → 全崩。
- 杀伤力：🔴 若现场网络不稳。

### T3. tesseract 子进程 5s 超时
- 比赛机 CPU 慢 → 每块 OCR >5s → 超时返回 None → 块全跳过 → 数量校验失败。4 块就是 20s+ 的隐性地雷。
- 防线：❌ 超时硬编码 5s，无重试。
- 杀伤力：🟡。

### T4. busy 拒绝被算一次失败
- 竞赛软件连点/上一任务没结束 → busy → success=false → 浪费一次机会（共 2 次）。
- 防线：⚠️ 锁设计正确；❌ 无排队（busy 是设计选择）。
- 杀伤力：🟡。

---

## 六、🟡 代码微陷阱（魔鬼可以埋的针）

- **JSONL 日志不轮转不清理**：几天狂跑测试 → 磁盘写满 → 服务崩。比赛当天量小，但"logs/"在项目目录里，拷 U 盘时跟着走。
- **`OMPL` 子串判断**：臂消息若改文案（"planning succeeded via OMPL planner" 也含 OMPL ✓ 能命中；但若用别的词如 "RRT"）→ 静默 OMPL 不告警。业务层不查 OMPL 只靠客户端告警（进度总览自己记了）。
- **`hand.errors()` 字段假设 `error_codes`**：固件返回字段名变 → watcher 线程挂 → 8.4 失效且无感。
- **`debug_console.py` 的 `except (ArmError, Exception)`**：冗余写法（等价 except Exception），无害但说明没细查。
- **site_check 的相机检查 `Vision()` 不传内参**：capture 不需要内参 ✓ 无碍，但内参未标定不会被相机检查发现（config/summary 会）。
- **`_to_float` 对 `"0.0l"` 之类**：转成占位符 → 启动不报错，任务时才拒动——现场表现为"标定了还是拒动"。
- **tesseract 候选链含 `D:\OCR`**：比赛机没有 D:\OCR 就跳过 ✓ 无碍，但若比赛机恰好有个**没 eng 数据的 D:\OCR\tesseract.exe** → 优先命中它 → "退而求其次" → OCR 全灭。

---

## 七、🔴 人为流程（最大的魔鬼其实是自己）

- **verify_known_point 可跳过**（清单 4.4 未勾）——系统性标定错误（棋盘格边长、坐标约定）直接上场，5mm 级错误让机械臂抓空/撞槽。
- **现场禁止编程调试**——任何 bug 只能在赛前发现；赛前唯一的"改错机会"就是调试清单 11 步，**少走一步都可能致命**。
- **30 秒内要点"开始"**——服务没起/没验证 → 白扣 5 分 + 仓促开跑。
- **拷错/拷旧版本**——U 盘里的 work 目录过期，现场跑的是旧代码。
- **开赛前没人跑一次 28 项自检 + site_check**——所有防线都依赖这一个动作。

---

## 魔鬼最爱的 Top 5 操作（按性价比）

1. **把旧版 site.yaml 覆盖回去**（dry_run=true 或坐标全占位）——一个文件，四道防线全依赖"有人核对"。
2. **比赛机用 conda 环境**——`import aiohttp` 崩，现场无外网修不了。
3. **灵巧手型号没确认**——O10/DexHand 字段对不上，手全崩，三赛题全灭。
4. **tesseract 装个旧版或只有 chi_sim**——task2 OCR 静默错/全灭，数量校验可能还骗过。
5. **safe_box 或手眼矩阵填错**（手滑）——物理碰撞，任务直接判失败。

> 一句话总结：**代码本身已经很扛打（占位符防御、拒算、自检 28 项），但所有防线都收敛到"现场有人按清单核对"这一个动作上——魔鬼不需要黑进代码，只需要让你跳过核对。**
