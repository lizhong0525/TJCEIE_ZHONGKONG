# 文档核对报告：任务清单.md × 进度总览.md × 代码现实（2026-08-16 夜）

核对方法：把两份文档的每一条声明与当前代码/测试实测结果逐一对照。

---

## 一、先说清楚一个时间线（关系到信任问题）

| 时间 | 事件 |
|---|---|
| 16:41–16:50 | 你第三轮修复（A1–A4/B1/B2 等）+ 更新文档 |
| **18:25** | **第四轮代码修复**：task2.py（重拍选帧）、planner（safe_home 半标定） |
| 18:29 | README / 进度总览补"第四轮"记录 |
| 19:06 | 我的 `Bug检查报告-0816.md` |

**我的 bug 报告里 BUG-3（task2 重拍）、BUG-5（safe_home）标为"仍存在"是错的**——我引用的是 16:43 的旧读档，而你在 18:25 已经修完。实测当前代码：
- `task2.py:84-89`：恰好识别够 `expected` 优先、其次更接近——✓ 与第四轮描述一致
- `planner/__init__.py:69-76`：三轴全占位才回退默认位，部分填写抛错指明缺哪根轴——✓ 一致
- `task2.py:76-78`：报错文案去掉写死块数——✓ 一致

→ **第四轮的三条声明全部属实，代码比我的报告新。** 已修正。

## 二、进度总览.md 与现实的偏差（按严重度）

### 🔴 偏差 1：06 的"5/5 过"与"回归全过"不实（文档没跟上 bug 检查）

- L60：「合成测试（数字 1–4 全识别对、四块同图检出，**5/5 过**）」——**今天实测 FAIL**（数字 2 识别为空，`test_task2_synthetic.py` 断言崩）。`task2_blocks.py` 和测试文件都还是早上 9:36 的原样，没修过。
- L32 / L77：「02/03/05/06/07 回归不受影响」「回归全过」——06 挂，这条不成立。

**修法**：06 的 OCR 管线对齐 01（BUG-1）或至少把测试渲染改成 01 自检同款（BUG-2），修完重跑再改文档。当前"5/5 过"是假的。

### 🟡 偏差 2："专项 7 项全绿"无测试文件实锤

L32「自检 15 项 + 专项 7 项全绿」——15 项自检实测属实（我跑过 PASS），但**"专项 7 项"在仓库里找不到任何对应测试脚本/场景**（01 只有 service_selftest.py，无新增测试文件）。如果那是临时手动验证，建议：要么补进 selftest 变成可复跑的场景（重拍 3 种选帧、safe_home 2 种、expected_count 文案 2 种，正好 7 个），要么在文档里注明"临时验证未固化"。否则三天后没人知道这 7 项验了什么。

### 🟡 偏差 3：04 的"15 项合成功能测试全过"无测试文件

L50 / `04-相机/审查结论.md`：「重跑 15 项合成功能测试（亮灯/OCR/形状/坐标链/手眼求解还原）全过」——04 目录实际只有 `tools/selftest.py`（8 项断言）+ `tools/test_task1_lamp.py`（6 项），**凑不出 15 项**，源仓库 `运行相机检查/` 也没有。要么补测试，要么把"15 项"改成"8 项自检 + 6 项亮灯 + 手眼求解还原验证"这种能对得上的说法。

### 🟢 偏差 4：小问题

- L70「mock 实跑验证（health/task1/task2 全 success）」——**task3 没验**。mock 明明支持 task3，顺手补一句。
- L33「还剩啥」没提 OCR 脆弱性（BUG-1/BUG-2）和 mock 空 body 不一致（BUG-4）——19:06 的 bug 报告里的有效结论（BUG-1/2/4）需要记入文档，建议补"第五轮"记录。
- 其余（02 的 OMPL 提醒、03 方向验证、04 真机链、05 二选一、07 3D 位姿、08 WPF 超时实测）都与代码/README 一致 ✓。

## 三、任务清单.md 的 [x] 标记核对

| 条目 | 标记 | 核对结果 |
|---|---|---|
| 1.1 四接口 + 兜底 + 日志 | [x] | ✓ 属实（server.py 4 端点 + 中间件兜底 + LOG） |
| 1.2 mock 运行 | [x] | ✓ 属实（08 mock 存在并跑过） |
| 5.2 亮灯检测 | [x] | ✓ 属实（01 + 05 双实现，05 测试 6/6 过） |
| 5.4–5.8 按钮/拨动/组装/无状态/兜底 | [x] | ✓ 属实（task1.py + 自检 push/toggle 断言） |
| **6.2 数字识别** | [x] | ⚠️ **偏乐观**：01 生产管线常规渲染 1–4 全对，但 06 模块（同源副本）合成测试 3/4 识别失败、06 测试今天必挂。建议改注"初版通过（01 管线），现场图像集验证未做" |
| 6.6 严格按 1→2→3→4 | [x] | ✓ 排序实现，但 OCR 误读（3→5）会静默排错序——重拍只校数量不校内容，文档这句"没把握就重拍"实际是"数量不对才重拍" |
| 6.7 单块失败重试 | [x] | ✓ 属实（grasp_retries 可配） |
| 7.7 单块失败不中止 | [x] | ✓ 属实（task3.py + 自检 6b 断言） |
| 8.1/8.4/8.5/8.6 | [x] | ✓ 全属实（自检覆盖） |
| 4.4 工具已备好 | 备注 | ✓ verify_known_point.py 存在 |
| 1.2 其余 4 项 / 2.x / 3.x / 4.x / 6.1/6.3–6.8 / 7.x / 8.2/8.3/8.7 / 9.x | [ ] | ✓ 全是【现场】或待办，与代码一致 |

**任务清单本身没大问题**，唯一要动的是 6.2 的 [x] 太满（以及 6.6 的"重拍"描述建议加一句"只校数量"）。

## 四、结论与建议动作

1. **修 06（BUG-1/BUG-2）→ 重跑 06 测试 → 再改 进度总览 L60/L77**——这是文档与现实最大的一处矛盾，源头是代码没修。
2. 进度总览补"第五轮"记录：OCR 管线对比结论（01 全对 / 06 挂）、mock 空 body 不一致、以及"第四轮三条已核实"。
3. "专项 7 项"补成 selftest 场景或注明临时验证；04 的"15 项"改成可对账的说法。
4. 任务清单 6.2 改注；08 顺手补 task3 实跑记录。
5. 其余文档（02/03/05/07/08 各部分）核对无误，不用动。
