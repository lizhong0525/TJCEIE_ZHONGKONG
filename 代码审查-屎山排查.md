# 代码审查：已完成部分的屎山排查（2026-08-16）

审查范围：01-算法服务 / 02-机械臂 / 03-灵巧手 / 04-相机 / 05/06/07 赛题 / 08-联调测试，全部源码（不含 pycache）。
结论先行：**整体质量在同类型竞赛项目里算好的**——占位符防御、失败路径自检、"宁可失败不动臂"的红线贯彻得比大多数队伍强。
但确实存在 4 处**会翻车的逻辑漏洞**和一批**屎山级**的代码组织问题，按严重度排列如下。

> **修复状态总览（截至 2026-08-16 晚，轮次见 `进度总览.md`）**
>
> | 状态 | 条目 |
> |---|---|
> | ✅ 已修 | A1 A2 A3 A4 / B1 B2 B3 B4 B5 B7 B9 / B11 的双加载、π、t_end_camera 命名、args.save、旋转门禁、selftest 篡改项 |
> | 🔶 半修 | B6（OCR 已对齐，拷贝结构保留）、B8（删了 look_at/approach 等，placement_area 等保留）、B11 的 D:\OCR 项（加 eng 校验） |
> | ⏳ 未修 | B10（有意保留）、B11 的 hand.py 线程项（设计如此）与 _bootstrap 项 |
>
> BUG-1~BUG-5（19:06 那轮）在 `Bug检查报告-0816.md`，已全部修完。

---

## A 级：真·逻辑漏洞（比赛现场会白拿 0 分或误导排查）

### ~~A1. task3 全部"跳过"也报 success=true（假成功）~~ ✅ 已修（第三轮）
`01-算法服务/algorithm_service/tasks/task3.py:98-99`

```python
if not placed and failed:
    raise PickError(f"全部 {len(failed)} 个几何体处理失败…")
```

只拦了"**全失败**"，没拦"**全跳过**"。当 `classify_shapes` 输出的形状名与 `site.yaml` 里 `shapes.kinds[].name` 对不上（比如代码写 `square`、配置手滑写成 `squares`），或所有槽位已满时：
`placed=[]，skipped=[...]，failed=[]` → **success=true，一个块没放，竞赛软件判 0 分还以为是成功的**。
自检场景 6b 只测了"单块失败继续"，没测"全部跳过"。
修法：`if not placed: raise PickError(...)`（skipped 也算没完成）。

> **修复记录**：已按修法改为 `if not placed: raise`，message 附"形状名与 kinds 对不上"的诊断提示；selftest 新增场景 7b（kinds 为空→全跳过→success=false）锁回归。

### ~~A2. 01 的 Vision 手眼矩阵占位 → 静默退回单位矩阵（和已宣称的修复自相矛盾）~~ ✅ 已修（第三轮）
`01-算法服务/algorithm_service/vision/camera.py:52-55`

```python
try:
    self.hand_eye = np.array(hand_eye, dtype=np.float64) if hand_eye else np.eye(4)
except (TypeError, ValueError):
    self.hand_eye = np.eye(4)
```

site.yaml 的手眼矩阵是 `__现场标定后填入__` 字符串 → `np.array(..., dtype=float)` 抛 ValueError → **静默用单位矩阵**。
进度总览说"手眼/内参占位时不再静默用单位矩阵解算错坐标"——那只对 `tasks/_coords.py:48-52` 的路径生效（那边是抛 `PickError`），
**`Vision.pixel_to_base()` 这条路径把错误吞了**。将来"results→site.yaml 接线"时谁直接调 `Vision.pixel_to_base`，就会拿单位矩阵算出错误坐标还返回 success。
同一仓库两种行为，是典型的前后矛盾。修法：占位时抛 `VisionError`，别用 `np.eye(4)` 兜底。

> **修复记录**：`np.eye(4)` 兜底已删——矩阵非法时 `hand_eye=None`（capture 不受影响），`pixel_to_base` 直接抛 `VisionError`；docstring 写明接线约定 `t_base_end(拍照时刻) @ t_end_camera(标定输出)`。

### ~~A3. task2 的数量校验在默认配置下完全失效~~ ✅ 已修（第三轮）
- `config.py:242-258` 的 `_to_int` 把 `expected_count: __现场标定后填入__` 转成 **0**；
- `task2.py:71` `expected = _expected_count(cfg)` → 0；
- `task2.py:73` `(not raw) or (expected and len(raw) != expected)` → **expected=0 时长度永不校验**。

于是默认 site.yaml 下：识别出 3 个块（实际 4 个）→ 照放 3 个 → success=true。
而且"重拍重识别"只在**数量**不符时触发，OCR 把 2 认成 3（数量恰好对）也照放不误——与"没把握就重拍、绝不能猜"的自我承诺不一致。
自检场景 5b 是"四数字全对"，没测"数量不对但 expected 未标定"。

> **修复记录**：`expected <= 0`（占位/非法）直接拒动，数量校验必生效；selftest 占位场景锁住。第四轮又把提示文案里写死的块数去掉（expected_count 本身可配）。

### ~~A4. task2 重拍失败的报错数字是第一次的，不是重拍的~~ ✅ 已修（第三轮，第四轮加固）
`01-算法服务/algorithm_service/tasks/task2.py:79-83`

```python
elif not raw:                      # ← 判断的是第一次的 raw，不是 retry
    raise PickError(f"digit recognition failed: got 0 expected {expected}")
```

第一次识别空、重拍识别出 3 个（≠4）时，message 报 "got 0"，实际是 got 3。现场按报错排查会查错方向。行为本身不致命（反正失败了），信息是错的。

> **修复记录**：报错数字以"当选帧"为准。第四轮进一步修掉"取识别多的一帧"的选帧缺陷（首帧误检 5 个/重拍正确 4 个时会丢正确结果），现行规则：恰够 expected 优先，其次数量更接近 expected，打平保留首帧；selftest 3 个单元场景锁住。

---

## B 级：屎山代码 / 隐患（不修也能跑，但迟早咬人）

### ~~B1. task1 裸 `next()` → StopIteration~~ ✅ 已修（第三轮）
`01-算法服务/algorithm_service/tasks/task1.py:137`

```python
lamp = next(l for l in cfg.panel.lamps if l.name == lamp_name)
```

检测器返回的灯名和配置对不上（改名/大小写）时，抛裸 `StopIteration`，message 是 "no further elements"。
全项目到处在强调"失败 message 必须是人话"，这里漏了。selftest 没覆盖该路径。应改成循环查找 + `PickError("灯名 X 不在 panel.lamps 配置里")`。

> **修复记录**：已改循环查找 + 人话 PickError。

### ~~B2. config.py 手型解析不防占位符，全配置体系唯一例外~~ ✅ 已修（第三轮）
`01-算法服务/algorithm_service/config.py:420-425`

```python
open=[float(x) for x in poses_raw.get("open", [1.0] * 10)],
```

其它所有字段都走 `_to_float`（占位符容忍），唯独这里裸 `float()`：现场按惯例在 `hand.poses` 里写 `__现场标定后填入__` → **服务启动直接 ValueError 崩溃**。且列表不足 10 维不补全，运行时才在 `set_pos` 报"需要 10 维"。

> **修复记录**：新增 `_to_pose_list`，6 个手型全走它（逐项占位容忍 + 不足 10 维补全）。

### ~~B3. 手眼坐标系约定：04 求解的是 T_end_camera，01 要的是"基座→相机"~~ ✅ 已修（第六轮）
- 04-相机 `solve_hand_eye` 解出 **camera→末端** 变换（`vision/hand_eye.py:117-126`，用 `t_base_end @ t_end_camera` 链）。
- 01 的 `site.yaml:30` 注释写 `# --- 手眼（基座→相机）4x4 ---`，`camera.py:174-185` 直接把矩阵当 `T_base_camera` 用。

**现场把 04 标定结果填进 site.yaml 时，坐标系是反的/缺一环的**（眼在手上需要实时 `t_base_end`，01 现在没有这个参数）。进度总览说"接线未做"，但这是下一步动作，接线时必踩。建议：01 的 `pixel_to_base` 改成与 04 一致的 `t_base_end @ t_end_camera` 链，site.yaml 语义改成 `T_end_camera`。

> **修复记录**：按建议统一——site.yaml `hand_eye` 语义定为 `T_end_camera`（04 结果原样填入）；`pixel_to_base` / `_coords.pixel_to_base_pose` 改为 `t_base_end @ t_end_camera` 全链，缺 `t_base_end` 直接拒算；`capture()` 拍照时刻自动读 `/api/pose` 补 `t_base_end`；selftest 3 个像素链场景锁数学（平移叠加/缺位拒算/Vision 同款）。剩的纯体力活：把 04 `results/*.json` 的值填进 site.yaml。

### ~~B4. 手型参数名遮蔽模块函数~~ ✅ 已修（第三轮）
`01-算法服务/algorithm_service/planner/__init__.py:105-112`

```python
def pick(..., hand_pose: str, hand_pose_table: dict[str, list[float]]) -> None:
```

参数 `hand_pose_table` 遮蔽了同文件里的模块函数 `hand_pose_table(cfg)`。task2/3 里 `poses = hand_pose_table(cfg)` 然后 `planner_pick(..., poses)`——两个同名东西，读代码必晕。改成 `poses: dict` 即可。

> **修复记录**：`pick`/`place` 参数改名 `pose_table`。

### ~~B5. 硬编码 SAFE_HOME 和 DEFAULT_RPY，违背自己的设计原则~~ ✅ 已修（第三轮 + 第六轮收尾）
- `planner/__init__.py:35` `SAFE_HOME = Pose(0.275, 0.0, 0.48)` —— 每次任务开场/收尾位，工作台高度或臂安装位置一变就要改代码。
- `arm.py:20` `DEFAULT_RPY = (-3.141, -1.552, 3.141)` —— 同上。

config.py 文档明说"代码中不写死任何长度数值"，这俩是例外。至少 SAFE_HOME 该进 site.yaml（或从 `safe_box` 中心推导）。

> **修复记录**：SAFE_HOME 第三轮已进 `service.safe_home`（第四轮加半标定防御）；DEFAULT_RPY 第六轮收尾——`service.default_rpy` 入 site.yaml，ArmClient 实例级默认姿态（`line_to`/`free_move` 的 `rpy=None` 时取用），02 副本同步。常量只作兜底默认。

### B6. 同一赛题两套视觉实现 + 整文件拷贝三份 🔶 半修
- task1 亮灯：`01/tasks/task1_vision.py`（HSV 颜色比例+灰度均值）**≠** `05/task1_lamp.py`（percentile 亮度分，与 04-相机同源）。进度总览承认"现场二选一"，但两套并存就是埋雷：现场调参调的是 05，真机跑的是 01。
- `04-相机/vision/task2.py` / `06/task2_blocks.py`、`04-相机/vision/task3.py` / `07/task3_shapes.py` 是**整文件逐字拷贝**，改一处漏三处。

> **当前状态**：OCR 段已对齐——06/04 的 task2 与 01 同一套管线参数（第五轮 BUG-1，"改一处漏三处"里最要命的一处消除了）。亮灯两套实现和拷贝目录结构保留：05/06/07 定位是模块工作台，正式服务以 01 为准；亮灯现场二选一仍挂在 05 README 和进度总览。

### ~~B7. 手型数据三处重复~~ ✅ 已修（第六轮）
`site.yaml hand.poses`、`01/config/hand/poses.yaml`、`03-灵巧手/poses.yaml`——同一份 10 维数组存在三个地方，同步靠自觉。

> **修复记录**：排查发现后两份根本没有代码读取（纯死副本）——已删除，手型唯一来源 = site.yaml `hand.poses`；03 README 和 bringup 提示语同步改。

### B8. 死代码 / 死配置 🔶 半修
- `placement_area`：config.py 解析了、site.yaml 有、calibrate.py 交互采集，**没有任何业务代码读它**。
- `planner.look_at` / `planner.approach`：定义了，task1/2/3 全没用（`approach` 与 `pick` 内部逻辑重复）。
- `arm.free_move` / `arm.status` / `arm.cancel`：01 业务层没人调（02 bringup 用了 status，03/04 有自己的客户端）。
- `server.py:180-181` `_ = hand_pose_table` —— 为了消 lint 警告 import 再丢弃，明摆着告诉读者"这里什么都没干"。

> **当前状态**：`look_at`/`approach` 和 `_ = hand_pose_table` 已删（第三轮）。`placement_area` 保留（calibrate 采集链路要用）；`arm.free_move/status/cancel` 保留（客户端能力，02 bringup 在用 status）。

### ~~B9. 单像素深度 vs 5×5 中值深度，同一件事两种做法~~ ✅ 已修（第三轮）
`_coords.py:19-28` `center_depth_m` 取**单像素**深度；04-相机 `geometry.py:51-59` `robust_depth_at` 取 5×5 中值。真机深度噪声下 01 的更脆，块中心恰好压在数字凹坑上直接取到坏值。应统一成中值。

> **修复记录**：`_coords.center_depth_m` 已改 5×5 中值，与 04 对齐。

### B10. 服务器把异常原文透传 ⏳ 未修（有意保留）
`server.py:72` `return _fail(str(e))`——内部 HTTP 细节/路径直接进 message。竞赛软件只看 success 问题不大，但属于坏习惯。

> **当前状态**：保留。竞赛软件只判 success；message 里的原文是现场排查的唯一信息来源，人话化已经在抛错点做过了。

### B11. 小屎汇总（不值得单列）
- ~~`server.py:191-192` `run_app` 先 `load_cfg` 一次、`app_factory` 里又 `load_cfg` 一次，双重加载。~~ ✅ 已修（第六轮，`app_factory` 加 `cfg` 参数，已加载就直接用）
- `task2_vision.py:49` 硬编码 `D:\OCR\tesseract.exe`（有注释解释，但仍是机器特定路径写死在代码）。🔶 半修——候选 exe 必须带 eng.traineddata 才选用（本机 D:\OCR 只有 chi_sim 曾致 OCR 静默全灭），路径仍作候选保留
- `hand.py:114` `first_error` 后台线程写、主线程读，无锁（GIL 下能用，严格说有 data race；`__exit__` join 2s 后线程可能仍在跑）。⏳ 未修（设计如此，线程边界已写进文档）
- ~~`service_selftest.py:400/414/423…` 用 `report.items[-1]["ok"] = ... and xxx` 事后篡改报告项，能跑，丑。~~ ✅ 已修（第三轮，`expect()` 加 `extra_ok` 参数）
- ~~`solve_hand_eye.py:24-27` 质量门禁算了 `max_target_rotation_spread_deg` 却不查，只查重投影和位移。~~ ✅ 已修（第六轮，门禁加 ≤2° 检查；合成数据验证：小角度样本求解误差 0.09 会被拦，大角度样本还原 3.8e-10）
- ~~`hand_eye.py:124` 变量名 `t_end_camera`，实际内容是 **camera→gripper** 矩阵，命名反直觉（04 内部自己用对了，但谁读名字谁上当）。~~ ✅ 已修（第六轮，局部变量改名 `t_end_from_camera` 并注释约定；results JSON key 保持 `t_end_camera` 不变，工具链不受影响）
- ~~`check_camera.py:47` `args.save = False` 改 argparse 对象，靠副作用控制只存一次。~~ ✅ 已修（第六轮，局部变量 `save_pending`）
- ~~`task3_vision.py:59` 用 `3.14159` 硬编码 π（旁边就 import 了 math 的兄弟文件都在用 `math.pi`）。~~ ✅ 已修（第六轮，`math.pi`）
- `_bootstrap.py` sys.path hack：`import _bootstrap` 才能 `from vision.config import ...`，没有正经包结构，换个目录跑就崩。⏳ 未修（04 工具链结构，动了要回归采集/标定全流程，留到真机联调稳定后）

---

## 写得好的地方（免得你误伤）

- 占位符防御体系（`pose_from_vec3` / `ensure_safe` / `_coords`）贯彻得彻底，失败路径 message 基本全是人话。
- `service_selftest.py` 的"失败路径必须断言 success=false + 动作链断言"是真金白银，比大多数队伍的自检强。
- `errors_watch` 后台线程 + 调用方检查 `first_error` 的约定（文档写清楚了异常会被线程边界吞掉），设计诚实。
- 08 mock server 写得干净：参数化模式/延迟/限流，没有明显毛病。
- 04-相机 的手眼/台面/已知点验证链路完整，`verify_known_point` 补上了外部验收缺口。

---

## 建议修复顺序（按性价比）——执行情况

1. ~~A1（task3 全跳过假成功）~~ ✅ 已完成（改一行 + selftest 场景 7b）。
2. ~~A2（Vision 静默单位矩阵）~~ ✅ 已完成（占位抛错，`np.eye(4)` 兜底已删）。
3. ~~A4（重拍报错数字）~~ ✅ 已完成（第三轮）+ 选帧规则加固（第四轮）。
4. ~~A3~~ ✅ 已完成——占位直接拒动，比"LOG 大警告"更硬。
5. ~~B1、B2~~ ✅ 已完成。
6. ~~B5 SAFE_HOME 挪进 site.yaml~~ ✅ 已完成（DEFAULT_RPY 仍在待办）。
7. ~~接线 04→01 前先统一手眼坐标系约定（B3）~~ ✅ 已完成（第六轮，链统一 + selftest 锁数学；剩填值）。
8. 把 05/06/07 的拷贝改成 import 04 的模块，消灭三份副本（B6/B7）——🔶 部分完成（B7 死副本已删、OCR 段已对齐；05/06/07 结构整合留到真机联调稳定后再动）。
