# 比赛机环境完整清单与安装方案（Win11 · 现场无外网）

> 适用对象：比赛用工控机/比赛机（Windows 11 x64，现场无外网）。本文是"比赛机上要装的所有环境"的**唯一权威清单**：
> 系统软件、Python 运行时、pip 依赖、相机 SDK、OCR、项目文件、网络与硬件连接、安装步骤、验收标准与红线。
> 配套一键检查脚本：`09-现场部署/环境检查.bat`（双击即出结论，装完必跑）。
>
> **安装列表（2026-08-18 定稿，已核对适配 work 全部内容）**：
> - 系统：**python-3.11.9-amd64.exe**（官方安装器，勾 Add python.exe to PATH）+ **Tesseract 5.3.x**（UB-Mannheim，勾 English）
> - pip 6 包精确版本：aiohttp==3.11.11 · pyyaml==6.0.2 · numpy==1.26.4 · opencv-contrib-python==4.10.0.84 · requests==2.32.3 · pyorbbecsdk2==2.0.15
> - **不使用 conda**（历史 conda-pack 方案已废弃，见 `09-现场部署/打包conda环境.bat` 的废弃说明）。
> - **依赖冻结，不许加新包。** websockets 不在安装列表（代码未 import，pyproject 已移除声明）。

---

## 1. 环境总览（一页速览）

| 类别 | 装什么 | 版本要求 | 怎么装 | 不装 / 装错会怎样 |
|---|---|---|---|---|
| 系统层 | Win11 x64 自带 PowerShell / curl | Win11 即可 | 无需安装 | — |
| Python 运行时 | **python.org 官方 3.11.9 安装器**（唯一方案） | **3.11**（3.12 亦可但建议统一 3.11） | 离线安装器，勾 "Add python.exe to PATH" | 版本不对：pyproject `requires-python` 拒装/服务起不来 |
| pip 依赖 | 6 个包（见 §3.4 精确版本） | 见 §3.4 | `现场安装.bat` 离线装 wheels | 见 §3.4 每行"缺了会怎样" |
| OCR | Tesseract **5.x** + **eng.traineddata** | 主版本 ≥5 | 单独装（UB-Mannheim 安装包，勾 English） | 无 eng / 4.x：数字 OCR 静默全灭或读错，task2 全丢 |
| 相机 SDK | pyorbbecsdk2（自带运行库 DLL） | ==2.0.15 | wheels 随装；设备不识别时补 Orbbec SDK 运行时 | 三赛题全灭：camera not ready |
| 臂 / 手 | **不需要装任何驱动** | — | 只需网络连通 + requests 调 HTTP | 网线/IP 错：超时判负 |
| 项目文件 | 整个 `work/` 目录 + 标定产物 | — | U 盘拷贝 | 缺文件：服务起不来 / 占位符拒动 |
| 磁盘 | 剩余空间 | ≥1 GB（wheels 方案占用很小） | — | — |

---

## 2. 在家的准备工作（有网机器上，去现场前做完）

### 2.1 生成离线 wheels（有网机，唯一准备动作）

- 双击 `09-现场部署/下载wheels.bat` → 产物 `09-现场部署/wheels/`。
- 内部命令（精确版本，6 包）：
  ```text
  python -m pip download aiohttp==3.11.11 pyyaml==6.0.2 numpy==1.26.4 opencv-contrib-python==4.10.0.84 requests==2.32.3 pyorbbecsdk2==2.0.15 -d wheels --platform win_amd64 --python-version 3.11 --only-binary=:all:
  ```
- 某精确版本拉不到时：去掉 `==版本号` 改用 `>=` 下限（aiohttp>=3.9、pyyaml>=6.0、numpy>=1.26,<2、opencv-contrib-python>=4.8、requests>=2.31、pyorbbecsdk2>=2.0.15）自动取最新。
- `--python-version` 必须 = 比赛机 Python 版本（统一 3.11，勿改 3.12）。

### 2.2 下载系统安装包（U 盘要带）

1. **python-3.11.9-amd64.exe**（python.org 官方，64 位）。
2. **Tesseract 5.3.x 安装包**（UB-Mannheim 版，GitHub: `UB-Mannheim/tesseract` Releases）——安装时**必须勾选 English 语言数据**。
   - 验证：装完应有 `C:\Program Files\Tesseract-OCR\tesseract.exe` 和 `...\tessdata\eng.traineddata`。
   - 代码候选链：site.yaml 的 `digit_blocks.tesseract_command` 覆盖 → `D:\OCR\tesseract.exe` → PATH → `C:\Program Files\Tesseract-OCR\tesseract.exe`（不带 eng 的不算数）。
3. **Orbbec SDK 运行时**（备用，仅当相机插上不被识别时用；正常 pyorbbecsdk2 的 wheel 自带 DLL）。
4. 整个 `work/` 项目目录（= 本工作区，含 01–09 各模块 + 官方 mock 副本 + 测试工具）。

### 2.3 U 盘最终清单（走之前逐项打勾）

- [ ] `wheels/`（`下载wheels.bat` 产物，6 包精确版本）
- [ ] `python-3.11.9-amd64.exe`（官方安装器）
- [ ] Tesseract 5.3.x 安装包（**带 eng**，只有 chi_sim = 白带）
- [ ] `work/` 全目录（U 盘里再放一份备份）
- [ ] 标定产物备份：内参 / 手眼 / 台面 / config.json / site.yaml 新版
- [ ] 现场物品：标定板、位置标定贴纸（可选）、笔记本、网线、带供电 USB Hub（相机供电不足时用）
- [ ] **U 盘两份**（一份出意外还有一份）

---

## 3. 比赛机安装清单（按类别）

### 3.1 系统层（Win11 自带，确认可用即可）

| 项 | 说明 |
|---|---|
| PowerShell | 运行 .bat / 命令；自带 |
| curl | 现场 11 步、验收用；Win11 自带（`curl --version` 验证） |
| 磁盘空间 | ≥1 GB（wheels 方案占用很小，无 conda 解压需求） |
| 项目路径 | **建议拷到无中文的路径**（如 `C:\zkc_work\`）：中文路径下 `cv2.imwrite` 可能**静默失败**（返回 False），存图无文件 |
| 防火墙 | 竞赛软件与本服务同机（127.0.0.1:5000），无需放行入站；出站调臂/手 IP 首次弹窗点"允许" |
| 锁屏密码 | 交卷后要设（规则），提前想好 |

### 3.2 Python 3.11.9（唯一方案，官方安装器）

- 安装包：`python-3.11.9-amd64.exe`（python.org 官方，Win11 x64）。
- **安装选项：勾选 "Add python.exe to PATH"**（不勾 = 命令行找不到 python）。
- 装完验证：新开命令行 `python --version` → `Python 3.11.9`。
- **红线：比赛机不要装 Anaconda/Miniconda、不要用任何 conda 环境**——避免 PATH 污染与 ssl/ASN1 混装问题；官方安装器 + wheels 是最干净的组合。

### 3.3 Tesseract OCR（系统软件，唯一需要"单独装"的）

- 安装包：UB-Mannheim 版 **5.3.x**。
- **安装选项：勾选 Additional language data → English**（缺 eng.traineddata = OCR 静默全灭）。
- 主版本必须 **≥5**：4.x 的 `--psm 8` 行为不同，可能**读错数字而不是读空**（更阴险）。
- 装完手动验证：`& "C:\Program Files\Tesseract-OCR\tesseract.exe" --version` 首行 `tesseract 5.x`；`Test-Path "C:\Program Files\Tesseract-OCR\tessdata\eng.traineddata"` 为 True。
- 装了非常规目录 → 在 site.yaml 的 `digit_blocks.tesseract_command` 填完整路径。

### 3.4 pip 依赖全表（冻结，精确版本，`现场安装.bat` 离线装）

| 包 | 精确版本 | 降级下限（拉不到时） | 用途 | 缺了 / 装错会怎样 |
|---|---|---|---|---|
| `aiohttp` | ==3.11.11 | >=3.9 | 算法服务 HTTP 4 接口 | 服务起不来，全灭 |
| `pyyaml` | ==6.0.2 | >=6.0 | 读 `config/site.yaml` | 启动即崩 |
| `numpy` | ==1.26.4 | >=1.26,<2 | 全部视觉/位姿计算 | 2.x：与现有代码 API 不兼容，任务报错 |
| `opencv-contrib-python` | ==4.10.0.84 | >=4.8 | 图像、**findChessboardCornersSB** | 普通 opencv-python 没有 `findChessboardCornersSB` → 手眼标定/棋盘格全挂 |
| `requests` | ==2.32.3 | >=2.31 | 调臂/手 HTTP | 硬件全断 |
| `pyorbbecsdk2` | ==2.0.15 | >=2.0.15（导入名 `pyorbbecsdk`） | Gemini335 相机彩色/深度/内参 | 三赛题全灭（camera not ready）；health 仍 200，**静默** |
| `websockets` | **不装** | — | pyproject 声明已移除，代码未 import | 不影响现状 |
| `pytest` | dev only | — | 在家跑单元测试 | 比赛机不用装 |

### 3.5 相机 SDK 与驱动（Gemini335）

- 运行库 = `pyorbbecsdk2` wheel 自带（`libOrbbecSDK` DLL 等），**无需单独装 SDK 运行时**；Gemini 330 系列（含 335）自 pyorbbecsdk2 2.0.9+ 支持。
- 若插上相机设备管理器里**根本不出现**：补装 Orbbec SDK 运行时（U 盘备用）；Windows 11 对 USB3 相机一般免驱。
- 硬件注意：**必须插 USB3 口**；供电不足 → 画面卡/深度异常 → 换后置口或带供电 Hub。**USB 供电不足现场不可修，属致命级**。
- 验收：`python 04-相机/tools/check_camera.py --save` 出窗口、`output/` 能存图、中心深度随手掌变化。

### 3.6 项目文件与配置

- 拷贝整个 `work/`（含 `01-算法服务/` … `09-现场部署/`）。
- 相机配置：`04-相机/config.json`（现场从 `config.example.json` 复制，IP/标定结果只写 config.json）。
- 服务配置：`01-算法服务/config/site.yaml`（臂 IP / 手 IP / 标定值 / 手型 / tesseract_command 等；**现场标定后填**，占位符 = 拒动防线，别删）。
- 标定产物目录：`04-相机/results/`（内参 / 手眼 / 台面平面），全部备份 U 盘。

### 3.7 网络与硬件连接（臂 / 手 / 相机不需要装任何软件）

| 设备 | 地址 | 说明 |
|---|---|---|
| 竞赛软件 → 本服务 | `http://127.0.0.1:5000` | 同机回环，无网络依赖 |
| B9 机械臂 | `http://<臂IP>:8087` | 独立主机提供 HTTP，比赛机只做客户端 |
| O10 灵巧手 | `http://<手IP>:8088` | 同上；**先确认是 O10 不是 DexHand**（字段 `position` vs `positions`） |
| Gemini335 相机 | USB3 直连比赛机 | 唯一需要本地 SDK 的硬件 |

- IP 用静态或记录 DHCP 分配；**换 IP 就改 site.yaml**（现场网络不稳/换 IP = 超时判负风险）。
- 现场网络差（WiFi 干扰）等同致命级：能拉网线就拉网线。

---

## 4. 安装步骤（比赛机现场，按顺序，约 10 分钟）

1. 装 **python-3.11.9-amd64.exe**（勾 "Add python.exe to PATH"）。
2. 装 **Tesseract 5.3.x**（勾 English；默认装到 `C:\Program Files\Tesseract-OCR\`）。
3. 把 `wheels/` 与 `work/` 拷到比赛机（建议无中文路径，如 `C:\zkc_work\`）。
4. 双击 `09-现场部署/现场安装.bat`：自动完成 预检 Python 版本 → 离线装 6 包（精确版本，缺包自动降级 >=）→ import+ssl+版本红线冒烟 → pyorbbecsdk 冒烟 → tesseract eng/版本检查 → **自动跑 33 项端到端自检**。
5. 双击 `09-现场部署/环境检查.bat` 复核（Python 3.11 / numpy 1.x / contrib / tesseract ≥5 + eng / pyorbbecsdk / 磁盘）。

### 4.1 真机联调（环境装好之后）

照 `操作指南.md` 第 5 节现场 11 步：服务健康 → 臂连通 → 手连通 → 臂自检 → 手自检（0/1 方向人眼确认）→
相机取图 → 标定链（内参→手眼≥12 组→门禁→verify_known_point≤5mm）→ 填 site.yaml → 未标定清零 →
task2 全链 → task1/3。每步不过不跳下一步。

---

## 5. 装完验收（全绿才算装好）

| # | 命令 / 检查 | 预期 |
|---|---|---|
| 1 | `python -c "import aiohttp, cv2, numpy, requests, yaml, ssl; ssl.create_default_context()"` | 无输出无报错 |
| 2 | `python -c "import cv2; print(hasattr(cv2,'findChessboardCornersSB'))"` | `True`（contrib） |
| 3 | `python -c "import numpy; print(numpy.__version__)"` | `1.26.4`（1.x） |
| 4 | `python --version` | `3.11.9` |
| 5 | `python -c "import pyorbbecsdk; print('ok')"` | `ok`（相机 SDK） |
| 6 | `& "C:\Program Files\Tesseract-OCR\tesseract.exe" --version` | 首行 5.x |
| 7 | `Test-Path "C:\Program Files\Tesseract-OCR\tessdata\eng.traineddata"` | True |
| 8 | `python 04-相机/tools/selftest.py`（无相机） | 离线 8 项过（相机项 SKIP 可接受） |
| 9 | `cd 01-算法服务 && python -m tools.service_selftest` | **33 项全 PASS**（现场安装.bat 已自动跑） |
| 10 | `python 04-相机/tools/check_camera.py --save`（插相机） | 出图、能存图、深度随手掌变 |
| 11 | `curl http://127.0.0.1:5000/api/health`（服务已起） | `success:true` |

---

## 6. 环境红线（错一个全灭，装的时候反复核对）

1. **比赛机不装 conda**（Anaconda/Miniconda 都不要）：官方 Python 3.11.9 + wheels 是最干净组合；conda 混装（python 与 openssl 不同渠道）→ `import aiohttp` 崩 ssl/ASN1，无外网修不了。
2. **Tesseract 没有 eng / 主版本 4.x** → OCR 静默全灭或读错数字（1↔7、2↔3），task2 全丢；eng 数据比版本更要命。
3. **opencv 不是 contrib** → `findChessboardCornersSB` 不存在 → 手眼标定全挂。
4. **numpy 2.x** → 现有代码 API 不兼容，任务静默报错（钉死 1.26.4）。
5. **Python 版本**：必须 3.11/3.12（`requires-python >=3.11,<3.13`）；统一 3.11.9。
6. **中文路径** → `cv2.imwrite` 静默失败（返回 False），存图无文件 → 项目拷到无中文路径。
7. **相机 USB 供电不足 / 插错口** → camera not ready，三赛题全灭，现场不可修 → 用后置 USB3 口 + 带供电 Hub。
8. **pyorbbecsdk2 版本/位数与 Python 不匹配**（如 3.11 装成 3.12 的 wheel）→ 导入崩；版本必须与下载时一致。
9. **现场网络差 / IP 变了没改 site.yaml** → HTTP 全超时（臂 90s / 手 10s），等同判负。

---

## 7. 常见报错对照表

| 现象 | 原因 | 处理 |
|---|---|---|
| `python 不是内部或外部命令` | 装 Python 时没勾 Add to PATH | 重装勾 PATH，或手动加 PATH |
| `import aiohttp` 报 ssl / ASN1 | PATH 里混入了 conda 的 python | 清 PATH / 重装官方 Python，比赛机不装 conda |
| `AttributeError: module 'cv2' has no attribute 'findChessboardCornersSB'` | 装了 opencv-python 而非 contrib | 卸掉换 `opencv-contrib-python==4.10.0.84` |
| `Failed loading language 'eng'` | 无 eng.traineddata | 装 English 语言数据，或把 eng.traineddata 放进 tessdata |
| task2 数字读空/读错 | tesseract 4.x / 无 eng / CPU 慢超 5s | 换 5.x + eng；看日志重拍 |
| `camera not ready` / 取不到帧 | pyorbbecsdk2 没装 / USB 供电不足 / 版本不匹配 | 装 ==2.0.15；换 USB3 后置口；核对 Python 位数 |
| `pip 找不到 wheel`（现场安装.bat 重试后仍失败） | wheels\ 不完整 / 版本与 3.11 不符 | 回家重跑 下载wheels.bat 补全再拷 |
| `cv2.imwrite 返回 False / 存图没文件` | 中文路径 | 项目拷到无中文路径 |

---

## 8. 现场还要带的非软件物品（顺带）

标定板（9×6 内部交点、格长实测）、位置标定贴纸 1 张（可选）、笔记本电脑 1 台、U 盘 2 份、
网线（能拉线就拉线）、带供电 USB Hub、物理急停位置确认、参赛证件/身份证。

---

*本文与 `操作指南.md`（打包部署/现场 11 步）、`遗留问题.md`（环境类红线）配套使用；环境类冲突以本文为准。*
