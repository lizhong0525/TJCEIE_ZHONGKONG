# 操作指南

从本地开发到比赛现场的全部操作。顺序：本地自检 → 对接验收 → 打包 → 标定 → 现场 11 步 → 比赛日。

## 1. 本地开发与自检

```powershell
cd 01-算法服务
python -m tools.service_selftest   # 33 项端到端自检（改代码后必跑，全绿才算完）
python -m algorithm_service        # 起服务 0.0.0.0:5000
python -m tools.site_check         # 30 秒汇总：臂/手/相机/服务/未标定清单
python -m tools.calibrate          # 交互式把标定值写进 config/site.yaml
python -m tools.debug_console      # 调试台：读位姿打印可粘格式、Jog、手型、拍照、模拟调接口
```

- 各模块离线/合成测试命令见 `README.md`「怎么跑」。
- 赛题 3 形状算法（7.2）开发主场在工作区根 `grasp_sorting_project/`：`python -m pytest`（11 项）。
- 任务失败先查 `01-算法服务/logs/YYYYMMDD.jsonl` 最后一行的 `result_message` + `elapsed_ms`。
- 服务行为怪 → `GET /api/config/summary` 看 dryRun 和未标定清单。

## 2. 对接竞赛软件验收（08-联调测试）

1. `python 08-联调测试/contestant_mock_server.py`（默认 127.0.0.1:5000）。
2. 打开竞赛测试工具（工作区根 `测试工具/ZhongkongCup.AlgorithmTester.exe`，不入库），
   Base URL 填 `http://127.0.0.1:5000`。
3. 依次点健康检查、赛题 1/2/3，确认全成功；`--task2-mode failure` 确认失败能显示。
4. **超时容忍度实测（决定真机动作拆多大）**：`--task1-delay 8` 起步，再试 60、120 秒。
5. 把 mock 换成真服务（`python -m algorithm_service`）重复 3–4。
6. 赛前彩排：3 个 task 各完整跑 2 遍（演示每项至多 2 次机会）。

mock 参数：`--taskX-mode success|failure|http-error|invalid-json|empty`、`--taskX-delay 秒`、`--port 端口`。
空 body（无内容）按 `{}` 放行，与真服务一致（08 副本已对齐，工作区根官方原版未动）。

## 3. 打包部署（09-现场部署，工控机无外网）

**方案（2026-08-18 定稿，安装列表）**：python-3.11.9 官方安装器 + Tesseract 5.x + 6 个 pip 包精确版本离线 wheels。
**不使用 conda**（历史 conda-pack 方案已废弃，`打包conda环境.bat` 现为废弃说明）。
**U 盘清单**：`wheels/`（下载wheels.bat 产物）、`python-3.11.9-amd64.exe`、Tesseract 安装包
（**必须带 eng 语言数据**——只有 chi_sim 会导致 OCR 静默全灭）、`work/` 全目录、奥比中光 SDK 运行时（备用）。
**比赛机安装的完整清单、版本要求、验收与红线见 `比赛机环境清单.md`**；装完环境先双击 `09-现场部署/环境检查.bat` 复核。

**在家（有网）**：

- 双击 `09-现场部署/下载wheels.bat` → 生成 `wheels/`（6 包精确版本：
  aiohttp==3.11.11 · pyyaml==6.0.2 · numpy==1.26.4 · opencv-contrib-python==4.10.0.84 · requests==2.32.3 · pyorbbecsdk2==2.0.15）。
- 某版本拉不到：去掉 `==版本号` 用 `>=` 下限（见脚本内备注）。
- 下载 `python-3.11.9-amd64.exe` 和 UB-Mannheim Tesseract 5.3.x 安装包，连同 `wheels/`、`work/` 一起拷 U 盘。

**现场（比赛机）**：

1. 装 `python-3.11.9-amd64.exe`（**勾 Add python.exe to PATH**）。
2. 装 Tesseract 5.x（**勾 English 语言数据**）。
3. 把 `wheels/` 与 `work/` 拷到比赛机（建议无中文路径，如 `C:\zkc_work\`）。
4. 双击 `09-现场部署/现场安装.bat`：预检 Python 版本 → 离线装 6 包（精确版本，缺包自动降级 >=）→
   import+ssl+版本红线冒烟 → pyorbbecsdk 冒烟 → tesseract eng/版本检查 → **自动跑 33 项自检**。
5. 双击 `09-现场部署/环境检查.bat` 复核。

## 4. 相机与标定链（04-相机）

**推荐顺序：检查相机 → 导出内参 → 采手眼样本 → 求解手眼 → 拟合台面 → 调任务识别。**
所有标定工具只读、绝不自动动臂；机械臂由操作员低速移动，物理急停放手边。

1. **准备**：`Copy-Item config.example.json config.json`（现场 IP/标定结果只写 config.json，不回写模板）；
   装 pyorbbecsdk2（与 Python 版本/系统严格匹配；导入名 pyorbbecsdk）。
2. **检查相机**：`python tools/check_camera.py`（S 存图，Q/Esc 退出）。验收：画面不卡、
   中心深度随手掌变化、`output/` 能存图。USB 供电不足换带供电 Hub/后置口。
3. **导出内参**：`python tools/export_intrinsics.py` → `results/intrinsics.json`。
   换相机/改分辨率必须重导，不能抄另一台的。
4. **手眼采样**：标定板默认 9×6 **内部交点**、格长按卡尺实测填（24.7mm 就填 `0.0247`，
   填错全场三维坐标带比例误差）；板子平整固定无反光。`python tools/collect_hand_eye.py`
   （不自动动臂，人移臂→停稳→按 Enter），**≥12 组（建议 15–20）**，位置和旋转都要散开，
   覆盖远近距离、左右上下、画面中心和边缘；模糊/遮挡样本不要。
5. **求解**：`python tools/solve_hand_eye.py` → `results/hand_eye.json` 的 `T_end_camera`。
   `quality_gate_passed=false` 不得用于真机（样本 rotation spread >2° 会被门禁拦下）。
6. **独立验收（不许跳）**：`python tools/verify_known_point.py --pixel 320 240 --true X Y Z`
   （已知点不参与标定；或 `--board` 把棋盘格放新位置量首内角）。误差 ≤5mm（`--tol` 可调）
   才允许视觉结果上真机——质量门禁只查内部自洽，查不出棋盘格边长填错这类系统性错误。
7. **台面拟合**：`python tools/fit_table_plane.py` → `results/table_plane_camera.json`
   （`inlier_ratio` 过低说明 ROI 障碍物多/选错，会拒绝）。
8. **填 site.yaml**：`hand_eye` 直接填 `results/hand_eye.json` 的 `t_end_camera` 原值
   （不用再乘任何链）；`t_base_end` 由服务拍照时刻自动读 `/api/pose` 补上。
   用 debug_console 读位姿直接打印粘贴，防手誊错位（负号、m 不是 mm、travel 0.005=5mm）。
9. **task1 ROI/阈值**：三灯各亮 + 不同环境光各采一批，`python tools/test_task1_lamp.py`
   出预览图，调 `minimum_brightness_score` / `minimum_winner_margin`。
10. **标定进度一键看**：`python tools/vision_calibrate.py`。

**视觉接入真机运动前的验收线**：彩色/深度稳定采集 30 分钟；内参来自当前相机当前流配置；
手眼门禁过 + verify_known_point 过；三灯各测 ≥30 次；无灯/多灯疑似/深度为空必须拒绝运动；
相机或位姿读取失败必须返回失败；所有第一次到点先到目标上方安全距离低速执行；
标定文件/配置/原始样本全部备份 U 盘。

## 5. 现场调试 11 步（标定日照着做，前一步不过别跳下一步）

**第 0 步先确认两件事（错了后面全白干）**：

1. **灵巧手型号**：`curl http://<手IP>:8088/api/status` 看字段——`position` 是 O10（:8088），
   `positions` 是 DexHand（:5001）。全套代码按 O10 写，若是 DexHand 立即停手改配置。
2. **比赛机环境冒烟**：`python -c "import aiohttp"` 必须过（比赛机是官方 Python 3.11.9 + wheels，
   一般不会出 ssl/ASN1；若出了多半是 PATH 混入 conda 的 python，清 PATH 后重跑 `09-现场部署/现场安装.bat`）。

| # | 步骤 | 命令 / 操作 | 预期 | 常见失败 |
|---|------|------------|------|---------|
| 1 | 服务健康 | `curl http://127.0.0.1:5000/api/health` | `success:true` | 服务没起；aiohttp 崩（见第 0 步） |
| 2 | 臂连通 | `curl http://<臂IP>:8087/api/status` | 返回 JSON | IP/网线；DHCP 换 IP 就改 site.yaml |
| 3 | 手连通 | `curl http://<手IP>:8088/api/status` | 返回 JSON | 同上；**先确认 O10/DexHand** |
| 4 | 臂自检 | `python 02-机械臂/arm_bringup.py --host <臂IP>` | status→motors→enable 全过 | TF 未就绪等几秒；enable 失败查急停 |
| 5 | 手自检 | `python 03-灵巧手/hand_bringup.py --host <手IP>` | errors 全 0 + **0/1 方向人眼确认** | 方向文档自相矛盾，必须真机看一次 |
| 6 | 相机取图 | `python 04-相机/tools/check_camera.py --save` | 窗口出彩图，output/ 存图 | SDK 没装；USB 供电不足 |
| 7 | 标定链 | 内参 → 手眼 ≥12 组 → 求解门禁 → verify_known_point | 门禁 PASS + 已知点误差内 | 样本角度太集中；spread>2° 被拦；验收不许跳 |
| 8 | 填 site.yaml | debug_console 读位姿直接打印粘贴 | 所有占位填完 | 手誊抄错位（负号 / m 不是 mm / travel 0.005=5mm） |
| 9 | 未标定清零 | `curl http://127.0.0.1:5000/api/config/summary` | `uncalibrated:[]` 且 `dryRun:false` | 漏填项全列在这个端点里 |
| 10 | 赛题 2 全链 | debug_console 调 `POST /api/task2/execute` | 4 块按 1→2→3→4 入槽 | OCR 读错（换角度/曝光重拍）；数量不符看 JSONL |
| 11 | 赛题 1→3 | 同上依次调 task1 / task3 | `success:true` | 灯 ROI 阈值；形状名与 kinds 对不上会全跳过报错 |

顺手在第 10 步后实测超时容忍度（`--taskX-delay 8/60/120`），决定动作拆多大步。

**上场前最后核对（30 秒）**：

1. `GET /api/config/summary` → `uncalibrated` 为空、`dryRun` 是 **false**（他队的坑：
   dry_run 忘关，接口全 success 但机器人不动）。
2. 服务启动日志**没有** `⚠️ DRY-RUN` 大红字。
3. `logs/今天日期.jsonl` 在持续写入。
4. **site.yaml 是今天新版**（改完立刻备份到 U 盘另一个目录，防旧版覆盖回去）。
5. 物理急停按钮位置所有人都知道；`/api/disable` 会掉臂，别当普通停止键用。

**环境红线速查（比赛机无外网，错一个全灭）**：

- Python：官方 3.11.9 安装器（勾 PATH）+ wheels 精确版本（见 比赛机环境清单.md）；**比赛机不装 conda**。
- opencv 必须 **contrib**（`findChessboardCornersSB` 只有 contrib 有）；numpy 1.x。
- tesseract 主版本 ≥5 且 tessdata 有 `eng.traineddata`。
- 项目在中文路径下，`cv2.imwrite` 可能静默失败（返回 False）——存图后肉眼确认文件存在。

**排障先看哪**：任务失败 → `logs/YYYYMMDD.jsonl` 的 `result_message`；服务行为怪 →
`/api/config/summary`；坐标错 → 重跑 verify_known_point（别直接改代码）；
OCR 全灭 → 先查 tesseract 版本和 eng 数据，再换角度/曝光重拍。

## 6. 硬件备忘（写代码/排障前看）

**B9 机械臂（`http://<臂IP>:8087`）**：

- 目标位姿必须嵌套：`{"mode":"right_arm","right":{"x":..,"roll":..,...}}`（左臂换 `left_arm`/`left`）。
- 推荐姿态 `roll=-3.141, pitch=-1.552, yaw=3.141`；直线安全工作域（右臂）Y −0.28~−0.04 m、Z 0.44~0.52 m。
- REST 运动阻塞，服务器上限 60s（客户端 timeout ≥90s）；长动作拆小步或用 `ws://<IP>:8087/ws`（无 60s 限制）。
- `message` 含 `OMPL` = 直线回退成自由路径——客户端只告警不抛错，**业务层必须自己查**。
- 业务失败返回 HTTP 400，客户端抛 `ArmError`（原始 message 嵌在异常文本里）。
- `/api/cancel` 只是软标记停不了轨迹；`/api/disable` 立即断力掉臂。
- 关节顺序：shoulder_pitch / shoulder_roll / shoulder_yaw / elbow_roll / elbow_yaw / wrist_pitch / wrist_yaw；全零 = 自然下垂。

**O10 灵巧手（`http://<手IP>:8088`）**：

- 日常只用 `POST /api/set_pos`（10 维 [0,1]）；**不要用 `/api/set_motor` 极限值**（堵转）。
- 关节顺序（0–9）：thumb_roll / thumb_abad / thumb_mcp / index_abad / index_pip /
  middle_pip / ring_abad / ring_pip / pinky_abad / pinky_pip。
- 错误码 bitmask：1 堵转 / 2 过热 / 4 过流 / 8 电机异常 / 16 通讯异常；非 0 立即停手撤臂（8.4）。
- 抓取确认：夹持后轮询 `/api/pvc` 电流——电流升高但位置没到 = 夹到了；位置到底电流没起 = 抓空。
- 进阶限流：`POST /api/set_pvc` 带 `torque`（mA，0–1000），防夹坏道具。
- **0/1 哪个是张手必须真机确认一次**（官方文档 §3.1 与 §4.6 自相矛盾），结论写进代码注释。

## 7. 比赛日规则速记

详见 `任务清单.md` 第 9 部分。要点：08-18 16:00 前报到；开赛指令后 **30 秒内**点"开始"（逾期 -5 分）；
每项任务至多 2 次演示机会，演示全程**严禁编程调试**；设备真故障举手找工作人员（虚假申报扣分）；
提交验证码是大写 `OK`；交卷后登记总耗时、队长签字、给工控机设锁屏密码；异议 08-20 18:00 前反馈。
