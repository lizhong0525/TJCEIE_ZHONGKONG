# 修改点清单（魔鬼清单 → 可执行动作）

> 按优先级分组；每条给出：位置 / 现状 / 改法 / 防的魔鬼点。耗时是估计值。
> P0 = 比赛前必须做；P1 = 强烈建议；P2 = 顺手；P3 = 可选。

---

## P0 · 比赛前必须（防 🔴 级翻车）

### M1. 灵巧手型号自适应校验（防 F1：O10 vs DexHand 全崩）
- **位置**：`03-灵巧手/hand_client.py` + `algorithm_service/hardware/hand.py`（两处同步）+ `03-灵巧手/hand_bringup.py`
- **现状**：`status()` 直接 `r.json()` 透传，字段假设 O10（`connected`/`hand_type`/`position`/`error_codes`）；现场若是 DexHand（:5001/`positions`）→ 下游全 KeyError。
- **改法**：`status()` 解析时做结构校验——缺 `connected`/`hand_type` 时抛 `HandError("响应结构不符 O10 约定，疑似 DexHand 或固件不同：<keys>")`；`hand_bringup` 步骤 1 打印响应 keys 并主动比对；`pose()` 兼容 `position`/`positions` 两字段。
- **耗时**：30 分钟。**这是全场最大单点，优先于一切。**

### M2. task1 亮灯检测加"并列拒绝"（防 V1：反光静默选错灯）
- **位置**：`01-算法服务/algorithm_service/tasks/task1_vision.py` `detect_lit_lamp`
- **现状**：直接取最高分，无 margin 检查；05 版有 `minimum_winner_margin` 拒绝，01 版没有——两套行为不一致，而生产路径是 01。
- **改法**：取前两名，`margin < 阈值`（从 task1.yaml 读 `minimum_winner_margin`，默认 1.2 倍）→ 返回不唯一 → task1.run 报"亮灯结果不唯一"拒动。与 05 语义对齐。
- **耗时**：20 分钟。防"反光时按错开关丢 30 分"。

### M3. 环境版本断言（防 E2/E3：tesseract 旧版、opencv 非 contrib、numpy/Python 错位）
- **位置**：`09-现场部署/现场安装.bat` 第 2 步冒烟 + `01-算法服务/tools/site_check.py` 加一节
- **改法**：冒烟命令扩成：`python -c "import cv2, numpy; assert hasattr(cv2, 'findChessboardCornersSB'), '需要 opencv-contrib'; assert numpy.__version__.startswith('1.'), '需 numpy 1.x'; import sys; assert sys.version_info >= (3,11) and sys.version_info < (3,13)"`；tesseract 加 `--version` 主版本解析（≥5 才 OK，4.x 打 ⚠️）。
- **耗时**：20 分钟。防"标定日发现 findChessboardCornersSB 不存在 / OCR 行为不同"。

### M4. 手型/坐标录入的单位与符号合理性校验（防 S3：漏负号、m/mm 混淆）
- **位置**：`01-算法服务/tools/calibrate.py` `_ask_float`/`_ask_vec3`
- **改法**：`_ask_vec3` 录完做范围提示（`|y|>0.5m 或 z>0.7m 提醒"超出常规工作台范围，确认？"`）；`travel` 录入提示"单位米，0.005=5mm"；最终落盘前打印全部数值摘要 + 二次确认。
- **耗时**：30 分钟。防现场手滑。

---

## P1 · 强烈建议（防 🟠 级静默丢分）

### M5. 01 的 OCR 数字过滤（防 V3：0/5/9 误读静默排错序）
- **位置**：`01-算法服务/algorithm_service/tasks/task2_vision.py` `recognize_digits`（`_ocr_patch` 返回后）
- **改法**：与 06/04 对齐：`if digit is not None and digit not in (1,2,3,4): digit = None`。
- **耗时**：2 分钟。上上轮就提过，还没动。

### M6. `placement_order_target` 取值校验（防 S5：拼错静默反序）
- **位置**：`01-算法服务/algorithm_service/config.py` `from_dict`（digit_blocks 段）
- **改法**：取值不在 {ascending, descending} → LOG.warning + 强制 ascending（或抛错拒动，二选一，建议 warning + 归正，避免现场误写直接全拒）。
- **耗时**：5 分钟。

### M7. `expected_count` 与 slots 数量交叉告警（防 S6：填错数量）
- **位置**：`config.py from_dict`（digit_blocks 段）
- **改法**：`expected_count` 已标定且 != len(slots) → LOG.warning 明示（现场 4 块 4 槽是常规，不一致必有鬼）。
- **耗时**：5 分钟。

### M8. tesseract 候选链去"退而求其次"（防 E2：无 eng 数据时试跑全灭）
- **位置**：`01-算法服务/tasks/task2_vision.py::_tesseract_cmd` + `06/04` 副本
- **现状**：都不带 eng 时 LOG.warning 后**仍返回第一个候选**（"至少试跑"）。
- **改法**：改为返回 None（无 eng = 不可用，宁缺毋滥；task2 会走"识别 0 个→明确失败"，比 OCR 乱读好）。可保留一个 `--allow-no-eng` 环境变量开关给离线调试。
- **耗时**：10 分钟（三处同步）。

### M9. `_to_float` 拒绝 nan/inf（防 S4：瘫痪型失败）
- **位置**：`config.py::_to_float`
- **改法**：`math.isfinite(float(v))` 不满足 → 记 warning + 返回占位符（这样 collect_placeholders 会列出它，未标定清单能看见）。
- **耗时**：5 分钟。

### M10. JSONL 日志大小上限（防磁盘写满）
- **位置**：`server.py::_make_jsonl_logger`
- **改法**：写前查文件大小，>20MB 时改名 `YYYYMMDD_1.jsonl` 续写（或直接截断旧文件，日志价值在最近一天）。
- **耗时**：10 分钟。

### M11. 查询类请求网络重试（防 T2：网络抖动全超时）
- **位置**：`arm.py`/`hand.py` 构造 `requests.Session` 时挂 `HTTPAdapter(max_retries=Retry(total=2, backoff_factor=0.3, allowed_methods=['GET']))`——**只对 GET 查询重试，运动 POST 绝不重试**（重试运动指令 = 重复执行危险动作）。
- **改法**：Session 初始化处加 urllib3 Retry；`hand.errors()`（后台 watcher 用）尤其受益。
- **耗时**：15 分钟。

### M12. `hand_bringup` 步骤 1 输出响应原文 keys（防 F1 的排查辅助）
- **位置**：`03-灵巧手/hand_bringup.py`
- **改法**：`hand.status()` 前先打印 `sorted(resp.keys())`，一眼看出是 O10 还是 DexHand 结构。
- **耗时**：5 分钟。（与 M1 合并做）

---

## P2 · 顺手（防 🟡 级小坑）

### M13. HandEye docstring 修正（防接线误导）
- **位置**：`config.py:59`
- **改法**：`"""基座→相机的 4×4 变换矩阵。"""` → `"""T_end_camera（相机→末端）4×4；完整链 = t_base_end @ t_end_camera。"""`
- **耗时**：1 分钟。**提了三轮了，这次务必改掉。**

### M14. calibrate.py 补新配置项交互（防配置体系缺角）
- **位置**：`tools/calibrate.py`
- **改法**：service 段补 `safe_home`/`default_rpy`/`log_dir`/`dry_run` 的录入（default_rpy 逐轴问，dry_run 问 y/n）。
- **耗时**：20 分钟。

### M15. debug_console.py 三处小修
- **位置**：`tools/debug_console.py`
- **改法**：① 存图检查 `cv2.imwrite` 返回值，失败打红色提示（防中文路径静默丢图）；② `except (ArmError, Exception)` → `except Exception`；③ `int(__import__('time').time())` → 顶部正常 import time。
- **耗时**：10 分钟。

### M16. `site_check.py` 输出目录 ASCII 化（防 cv2 中文路径）
- **位置**：`tools/debug_console.py` 存图路径 `ROOT / "output"`
- **改法**：改用 `%TEMP%/zhongkong_debug/`（系统临时目录），或项目下建 `output/` 前验证可写。低优先，M15 的返回值检查兜底即可。
- **耗时**：5 分钟。

---

## P3 · 可选（有时间再动）

### M17. safe_box 大小关系校验
- `from_dict`：`x_min < x_max` 等不成立 → warning（标反了等于全拒动，提示性价值）。
- 5 分钟。

### M18. OMPL 关键词扩展
- `arm.py::line_to`：`("OMPL" in msg or "RRT" in msg or "回退" in msg)` 告警。
- 5 分钟。

### M19. task2 相同数字排序稳定性
- `task2.py` sorted 加第二键 `b.pick.x`，消除轮廓顺序抖动。
- 5 分钟。

### M20. 根路由/health 响应加 dryRun 标记
- `health` 响应带 `"dryRun": c.service.dry_run`，测试工具/人肉眼一眼可见。
- 5 分钟。（config/summary 已有，这个是双保险）

---

## 不属于代码修改的"修改点"（流程钉死）

1. **开赛第一件事：确认灵巧手型号**（M1/M12 做完了就看 status 输出）。
2. **上场前 30 秒核对**（现场调试清单已有）——特别是 dry_run=false。
3. **verify_known_point 必须做**（清单 4.4 从 [ ] 改 [x] 之前不算完）。
4. **WPF 超时容忍度实测**（08 第 5 步，60s/120s 延迟）——决定任务能拆多大步。
5. **比赛机禁止 conda**，现场安装.bat 跑通 28 项自检再上场。

---

## 建议执行顺序（总耗时约 2 小时）

1. **M1+M12**（手型号，30 分钟）→ 2. **M2**（并列拒绝，20 分钟）→ 3. **M5+M8**（OCR 两处，15 分钟）
4. **M3**（版本断言，20 分钟）→ 5. **M13**（注释，1 分钟）→ 6. **M6+M7+M9**（config 校验，15 分钟）
7. **M11**（查询重试，15 分钟）→ 8. **M10**（JSONL，10 分钟）→ 9. **M4**（calibrate，30 分钟）→ 10. **M14-M16**（顺手项）
11. P3 随缘 → 12. 全量自检回归（28 项 + 05/06/07 + 02/03 离线）
