# 决赛提交对比报告：team01_supcon-cup-2026-final × work（2026-08-16）

对比对象：
- **决赛提交**：`G:\中控杯\0816\team01_supcon-cup-2026-final\supcon-cup-2026-final`（汪汪队，FastAPI 单包，39 文件）
- **工作目录**：`G:\中控杯\0816\work`（01-算法服务 / 02-机械臂 / 03-灵巧手 / 04-相机 / 05/06/07-赛题 / 08-联调测试，149 文件）

结论先行：**两个项目是同一竞赛的两种形态**。决赛提交是"现场交付型"——部署/离线/现场工具齐全，但算法与测试较浅；work 是"开发工程型"——逻辑、失败路径、测试深度远超决赛版，但**缺 Docker 打包与现场部署/预热工具**（恰好是决赛版的长处）。建议：**算法与工程以 work 为准，把决赛版的部署与现场工具方案移植过来**，不要反向照搬决赛版的算法与测试。

---

## 一、总体对比表

| 维度 | 决赛提交（team01） | work（当前工程） | 谁更好 |
|---|---|---|---|
| Web 框架 | FastAPI + uvicorn，同步端点 | aiohttp，异步 + asyncio.Lock 互斥 | work（有并发保护） |
| 并发安全 | **无锁**，并发调用会竞态 | 全局锁 + busy 响应 | work |
| 配置 | config.py 硬编码字典，现场改代码 | site.yaml 配置体系，`__现场标定后填入__` 占位校验 + 交互补全 | work |
| 机械臂客户端 | arm_client.py（requests，mode=right_arm） | arm.py（同 API，支持 left/right、default_rpy 实例级、plan_only） | 基本同级 |
| 灵巧手客户端 | hand_client.py（DexHand :5001，`{\"positions\":[...]}`） | hand.py（O10 :8088，`{\"position\":[...]}` + errors_watch 后台错误监控） | work 多监控；**但端口/硬件型号不一致，需核实** |
| 数字识别 | EasyOCR + Grounding DINO 区域检测 | tesseract --psm 8 + 轮廓紧致裁剪 + 2x 放大二值化 + 重拍重试 | work 轻量无 GPU 依赖，且已实测 1-4 全对 |
| 形状识别 | CLIP ViT-B/32 零样本 + Grounding DINO（重模型） | OpenCV 几何特征（圆度/顶点数/长宽比） | work 轻量；两者都撑不住任意姿态（work 已注明 3D 点云是最大缺口） |
| 亮灯检测 | Grounding DINO 检测灯 + 颜色/亮度 | ROI 制（HSV 颜色比例 + 亮度兜底，多灯取最高） | work（无需检测模型，ROI 明确） |
| 手眼标定 | calibrate.py 三阶段半自动（input 交互），**结果正则注入源码** | 04-相机工具链：采集→内参导出→solve（旋转 spread 门禁 ≤2°）→台面拟合→**verify_known_point 外部验收**，JSON 直接填 site.yaml | work（闭环+门禁+外部验收） |
| 相机 | Orbbec SDK / OpenCV 占位 / **远程 HTTP 服务模式** | pyorbbecsdk2 + D2C 对齐 + 内参导出 | work 更全；决赛版的远程相机模式有参考价值 |
| 测试 | 语法/导入/坐标数学/路由 + **mock 全成功路径** | selftest 25 项（**失败路径断言**）+ 离线 11/9 项 + 合成 6/5/5 项 + 文档核对 | work 碾压 |
| 部署 | **Docker 镜像 + 模型预下载 + run_on_site.sh + 离线 wheels + 双 .bat** | 无 Docker、无部署脚本 | **决赛提交（work 缺口）** |
| 现场工具 | **preheat.py 预热 + hardware_check.py 自检 + debug_tools.py 交互控制台（含模拟竞赛调用）** | 02/03 bringup 自检、04 check_camera、tools/calibrate.py | **决赛提交更成体系（work 可移植）** |
| 服务根路由 | GET / 返回 ready（hermes 平台轮询 /） | 只有 /api/health，**无 GET /** | 决赛提交（平台轮询考虑） |

---

## 二、决赛提交中值得参考、可移植到 work 的点

### 1. Docker 打包 + 模型预下载（Dockerfile，最值得搬）
- CPU torch 走官方 CDN extra-index + 清华源；`HF_ENDPOINT=hf-mirror.com`。
- **构建期预下载 CLIP / Grounding DINO / EasyOCR 模型进镜像** → 现场零联网可跑。
- `build_image.sh`：构建→冒烟验证→`docker save` 导出 tar（U 盘携带）。
- work 当前无任何容器化；如果现场工控机是官方预装环境，不一定需要 Docker，但这份方案是"无外网现场"的兜底，值得保留为一个可选部署路径。

### 2. 现场离线安装/部署方案
- `download_wheels.sh`：在家用清华源按 `win_amd64/py310` 预下载全部 wheel → 现场 `pip install --no-index --find-links=wheels`。
- `现场安装-汪汪队.bat`：离线装依赖 + 拷模型缓存（huggingface cache + .EasyOCR）。
- `run_on_site.sh`：Win11 Docker Desktop 注意事项（`--network host` 不支持 → `host.docker.internal`；USB 直通受限 → 相机服务跑宿主机），健康检查验证。
- **关键架构点**：Docker 容器内访问宿主机上的机械臂(8087)/灵巧手 HTTP 桥 + 宿主机相机服务(5002)，通过 `CAMERA_SERVER_URL` 远程取图——work 的 Vision 如果未来容器化，这条链路可直接借鉴。

### 3. 现场工具三件套
- `preheat.py`：开赛前把所有模型载入内存，验证推理延迟 <3s。
- `hardware_check.py`：5 项硬件/模型/服务自检 + 汇总表（✅/⚠️/❌）。
- `debug_tools.py`：交互式控制台——读位姿（**直接打印可粘进 config 的坐标格式**）、Jog、手测试、相机拍照、**选项 11 模拟竞赛软件调用**（对 work 的 08 联调很有价值）。
- work 已有 02/03 bringup 与 04 check_camera，但缺"读位姿→填配置"的交互工具与"模拟竞赛软件"的入口；可参考其交互形式补一个 `tools/site_jog.py`。

### 4. README 的"已知风险与应对"表
坐标未知→标定脚本；模型加载慢→预热；无外网→镜像打包；相机 SDK 缺失→U 盘携带安装包；灵巧手 API 变更→接口集中适配。这种风险预案格式值得 work 在 README 里补一张。

### 5. GET / 根路由
决赛版 `app.py` 有 `GET /` 返回 ready，注释写明是平台 hermes 轮询需要。work 的 server.py 只有 /api/health。**需确认竞赛软件/平台是否轮询 /，若是则补一条根路由**（成本一行）。

---

## 三、决赛提交的坑（不要搬，其中一个是真 bug）

### 🔴 apply_calibration.py 的正则注入是坏的
`apply_calibration.py` 搜索：
`r"        # TODO: 现场标定后替换.*?return \(x_arm, y_arm, z_arm\)\n"`
但 `vision_manager.py` 实际源码注释是 `# ===== 标定参数（calibrate.py 完成后由 apply_calibration.py 自动替换）=====`，且 return 是 `return (float(x_arm), float(y_arm), float(z_arm))`（带 float() 包裹）。
→ **正则永远匹配不到**，脚本必然报"未找到需要替换的代码段"。说明该脚本从未在最终版源码上实跑过。work 的 JSON→site.yaml 注入方案比它稳，别参考这条路径。

### 🟠 mock 测试是"假绿灯"（work 第一轮就修掉的坑）
`test_with_mock.py` + `mock_hardware.py`：所有 mock 恒成功、`detect_lit_light` **随机**返回灯、无任何失败路径断言、无动作链断言。这恰好是 work 自检重写时消灭的"假绿灯"问题。**照搬决赛测试 = 把坑捡回来**。

### 🟠 无并发锁
FastAPI 同步端点无互斥，竞赛软件若并发调用 task 接口会竞态（两臂/两手互相打架）。work 的 asyncio.Lock + busy 保留。

### 🟠 task2/task3 的"识别→抓取"其实没闭环
- task2：像素识别结果只用数字排序，抓取坐标走 `CUBE_SLOTS["slot_positions"]` 预设，注释自认"实际中 cube 的像素坐标需要通过手眼标定转换，这里先用预设槽位坐标"。
- task3：抓取坐标用 `cx/640`、`cy/480` **线性映射**到工作区（无深度、无手眼）。
→ 决赛版三任务实际都是"预设坐标表演"，视觉只负责判定顺序/类别。work 的 task2/3 已接 `pixel_to_base_pose` + 深度闭环，比它先进，保持 work 路线。

### 🟡 灵巧手硬件型号与端口不一致（**必须现场核实**）
- 决赛版：傅利叶 **DexHand**，`HAND_BASE_URL = http://127.0.0.1:5001`，`set_pos` 字段 `positions`。
- work：**O10**，`hand_port: 8088`，`set_pos` 字段 `position`。
- 同一竞赛两份代码对灵巧手 API 的假设不同，**说明官方文档可能中途换过手**。现场务必确认实际硬件型号 + 端口 + 字段名，否则灵巧手直接全灭。这也印证了决赛 README"灵巧手 API 变更 → hand_client 接口集中适配"的风险项。

### 🟡 坐标全部是占位值
决赛 config.py 里所有开关/槽位/台面坐标是占位（0.30/-0.20 之类），README 也写明现场必须重测。work 的 site.yaml 同样全占位——两边都依赖现场标定，这点没有差距。

---

## 四、建议动作（按性价比）

1. **移植部署与现场工具**（决赛版长处，work 缺口）：
   - 参考 Dockerfile/build_image.sh/run_on_site.sh 整理一份 work 的容器化 + 离线部署方案（若现场可免 Docker 则保留为备选）。
   - 补 `preheat` / `hardware_check` / 交互调试（读位姿→填 site.yaml）+ "模拟竞赛软件调用" 入口（对接 08 mock 与 WPF 工具）。
2. **补 GET /** 根路由（若平台轮询 /）。
3. **核实灵巧手**：硬件型号（DexHand vs O10）、端口（5001 vs 8088）、字段名（positions vs position）——这是两份代码间最大的事实冲突，直接决定 03 和 01 的 hand 模块改哪个。
4. **不要动**：work 的测试体系、site.yaml 配置、并发锁、pixel_to_base 闭环、手眼门禁——这些都明显强于决赛版。
5. README 补"已知风险与应对"表（参考决赛版格式，内容用 work 自己的风险）。

---

## 五、一句话结论

**算法、工程、测试全部以 work 为准；从决赛提交里值得拿的只有三样：Docker+模型预下载的离线部署方案、现场部署/安装脚本（wheels/bat/sh）、以及预热/自检/交互调试三件套。另外必须核实灵巧手硬件型号与端口差异（5001 vs 8088），这是两份代码之间的最大事实冲突。**
